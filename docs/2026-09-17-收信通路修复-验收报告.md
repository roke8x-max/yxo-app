# 收信通路修复（方案 A 轮询 + UID 水位线）—— 独立验收报告

> 日期：2026-09-17（午后）｜ 验收人：芙蕾雅 ｜ 落码：OpenCode（**未 commit / 未 push**）
> 依据 spec：`docs/2026-09-16-收信通路修复spec(OpenCode)-方案A轮询.md`（v7.2）
> 方式：**全部由我自己实跑 / 实读**，不采信汇报结论；凡"汇报值"与"我的实测值"并列写出。

---

## 0. 结论（先说要点）

| | |
|---|---|
| **总判定** | ⚠️ **有条件通过 —— 有 1 个必须先解决的阻塞项**（`trustme` 未声明依赖，导致最有价值的那条真链路用例在本机**必失败**） |
| **代码主体** | ✅ 与 spec 逐条对上，尤其**最高危的"重扫起点受 `FORWARD_SINCE` 约束"**与**"水位只在成功/入队时推进"**两条正确 |
| **我实测的用例数** | `1 failed, 246 passed`（EXIT=1）—— **不是绿的** |
| **越界** | 无（未碰 WORKFLOW.md / AGENTS.md / 生产配置；`docs/2026-09-14-上线前执行清单` 的 `M` 确认是接手前已脏） |

---

## 1. 🔴 阻塞项：`trustme` 未声明依赖 ⇒ 真链路用例在本机必失败

**我的实跑（原始输出）**：

```
$ python -m pytest -q -p no:cacheprovider mailbots_next/tests/
FAILED mailbots_next/tests/test_poll_uid.py::test_true_link_smoke_tls_proves_callback
1 failed, 246 passed in 169.13s (0:02:49)          ← EXIT=1
```

失败点：

```
mailbots_next\tests\test_poll_uid.py:106: in __init__
    import trustme
E   ModuleNotFoundError: No module named 'trustme'
```

**实核依赖声明（三处都没有）**：

```
$ grep -rn "trustme" requirements-dev.txt requirements.txt mailbots_next/requirements.txt
（无命中）
$ python -c "import trustme"
ModuleNotFoundError: No module named 'trustme'
```

**为什么这是阻塞项（不是小事）**：挂掉的这条 `test_true_link_smoke_tls_proves_callback` **正是本次事故的对症药** —— 它用**真 TLS + 真 `imaplib` 客户端**跑完整收信链路并断言"确有邮件被回调"（`calls >= 1`），专门防"测试全 mock 把'方法根本不存在'掩盖成全绿"这一课（9-16 IDLE 事故正是这么发生的）。
**结果变成：唯一能抓住那类事故的用例，成了唯一挂掉的用例。** 而它在第三方环境里"绿"，会掩盖问题。

> OpenCode 报的 **247 passed** 在他们装了 `trustme` 的环境里成立；**在本项目指定的 Python（`C:\Users\Roke8x\.workbuddy\binaries\python\versions\3.13.12`）上不可复现**。

**三条修法（**待洋拍板，我不擅自改**）**：

| 方案 | 做法 | 代价 |
|---|---|---|
| **A（推荐）** | `trustme` 进 `requirements-dev.txt`，并装进项目 python | 最小；多一个仅测试用的依赖 |
| B | 去掉第三方依赖：改用标准库 `ssl` + 内置自签 PEM（或运行期 `openssl`） | 测试代码稍长，但**零新依赖**、更自包含 |
| C | `pytest.importorskip("trustme")` | ❌ **不推荐**：会把最有价值的用例**静默跳过**，等于把病藏回去 |

---

## 2. ✅ 我实跑的其余判据（逐条）

### 2.1 grep 对账

| 断言 | 我的实测 |
|---|---|
| `conn.idle\b` | **0** ✓ |
| `_idle_once` / `MAX_IDLE_SEC` / `IDLE_PER_FOLDER` / `poll_fallback_secs` | **0** ✓ |
| `Idler` / `idle_processors` / `_idlers`（spec §1.8 约束1） | **0** ✓ |
| `idle_check` / `idle_done` —— **生产代码** | **0** ✓ |
| `idle_check` / `idle_done` —— **全仓** | **2**（均在 `tests/test_p1_coverage.py:884-885`，是 `assert not hasattr(conn, "idle_check")` 这类**否定断言**，非可执行调用）⇒ 良性，但 spec §5.1 的字面判据需注明"按生产代码口径" |
| 剩余 `idle`（不分大小写） | 逐条查过：`IDLE_GROUPS`/`get_idle_groups`（**文件夹分组**常量，非 IDLE 协议）、"IDLE 已彻底删除"注释、测试类名与否定断言 —— **无可执行调用** ✓ |
| `UNSEEN`：`core/ingest.py` | **0** ✓ |
| `UNSEEN`：`core/bounce.py` | **2**（`:283` 注释、`:285` 搜索）＝ **spec 预先写明的既定例外** ✓ |
| `settings.py`：`INGEST_POLL_SEC` / `IMAP_STATE` | 均出现（`:54/57/58/61`）✓ |
| `ingest.py`：`IMAP_STATE` / `UIDVALIDITY` / `uidvalidity` | 均出现 ✓ |

### 2.2 启动日志与拓扑（**我自己跑出来的，非采信**）

我把 `serve.Poller` 换成桩（不碰真 IMAP）后调 `start_pollers`：

```
[INFO] mailbots_next.serve: Started 12 ingest pollers (poll 30s)
[INFO] mailbots_next.serve: Ingest mode: poll 30s | connections=12 | folders=5 | discovery=uid-watermark
实际条数 = 12 ｜ 每账号出现次数 = {maoxiaoyang:3, yangyawen:3, fengqian:3, hanwenhao:3}
```

⇒ 与 spec §5.2 要求的那一行**逐字一致**，且**每账号恰好 3 条**（3 组 × 4 账号）✓

`poll_secs` 兜底：`Poller(poll_secs=0)` → 30；`poll_secs=-5` → 30（两处 WARN）✓

### 2.3 最高危的两条（我逐行读码确认）

| 要求（spec Step 1.9） | 实现 | 判定 |
|---|---|---|
| **重扫起点不是 `UID=1`，而是受 `FORWARD_SINCE` 约束** | `need_rescan` 时：有 `forward_since` ⇒ `UID SEARCH SINCE <date>`；**否则才** `UID 1:*` | ✅ 正确（`ingest.py:397-405`），附注释说明 |
| **水位推进只在 (A) 成功 或 (B) 已入队** | `ok` 为真才 `_advance_watermark`；`ok=False` ⇒ ERROR + counter + 企微告警且**不推进**；致命异常 ⇒ **先 `has_pending_message` 幂等检查**再兜底入队，入队成功才推进 | ✅ 正确（`:496-508`、`:466-494`） |
| **首跑（state 空）受门禁** | `saved_validity is None` ⇒ `need_rescan` ⇒ 同上 SINCE 路径 | ✅ |
| `UIDVALIDITY` 取自 `untagged_responses`，取不到 → WARN + 保守重扫 | `_get_uidvalidity()` 先读 `untagged_responses`，再兜底 `conn.response("UIDVALIDITY")`，都失败返回 `None` ⇒ 调用方按"已变化"处理 | ✅ |
| 取信一律 `BODY.PEEK[]` | `conn.uid("FETCH", str(uid), "(BODY.PEEK[])")` | ✅（顺带闭环 Q1） |
| **真 UID 语义**（最易漏那条） | `MarkSeenBatcher._flush_bucket` 用 **`conn.uid("STORE", seq, "+FLAGS", "(\\Seen)")`**；`fetch_raw_by_uid` 用 **`UID FETCH`** | ✅ |
| 每轮遍历**组内所有**文件夹 | `_poll_loop` 内 `for folder in self.folders:`，每轮 `sleep(poll_secs)` 恰一次；旧的内层死循环 `while` 已除 | ✅ |
| 保留重连结构 | 外层 `while not stop` + `try/except` + `_connect()` + `finally: close/logout` + 异常 `sleep(10)` | ✅ |

### 2.4 `process_email` 契约（spec 里最容易写歪的一段）

实读 `serve.py:75-202` + **实核依赖函数存在且可调用**（我的铁律：测试全 mock 会掩盖"方法不存在"）：

```
$ python -c "from mailbots_next.core.dedup import has_pending_message, add_error; ... "
has_pending_message: (message_id: str) -> bool
add_error         : (message_id, row_key, stage, error_type, error_detail, error_id, ..., raw_bytes) -> int
调用一次: False
```

| 要求 | 实现 | 判定 |
|---|---|---|
| 返回布尔 | 所有分支 `return True`；文档串写明 False=未纳入 | ✅ |
| `_identify_type` 失败 ⇒ 内部入队 + `True` | `try/except` + `add_error(..., raw_bytes)` + counter | ✅ |
| `extract_email` 失败 ⇒ 同上 | ✅ | ✅ |
| 未分类 / 空行 ⇒ 入队（**幂等检查**）+ 通知 + `True` | 有 `has_pending_message(message_id)` 检查 | ✅ |
| 行级异常 ⇒ 入队（`has_pending_error(message_id,row_key)` 幂等）+ `queued++` | ✅ | ✅ |
| 只有致命才抛 | 无 `raise`，DB 类错误自然冒泡 | ✅ |
| `False` 分支：不推进 + ERROR + 告警 | `:499-508` | ✅ |
| 明细给 sweep：`_last_outcome` | `sweep_once` 兼容 bool/dict（`:669-678`） | ✅ |

### 2.5 其余

- **Step 2 运踪**：`decide_tracing` 兼容 **list（扇出，非空→HIT）** 与**单个（`success`→HIT，否则 MISS）**，四种形态全覆盖；`route_tracing` 末尾 `return results`（list）+ 早期失败返回单个 —— 两侧口径已一致 ✓
- **Step 3 首启自建**：`dedup.py:15`、`store.py:44`、`extractors/draft.py:100` 均 `mkdir(parents=True, exist_ok=True)` ✓
- **import 冒烟**（spec §1.8 约束2）：`import mailbots_next.serve; import mailbots_next.core.ingest` → **ok** ✓
- **README 与实现一致**：12 条轮询、`--poll-secs` 为唯一周期真值、`--once` 标"未实现勿用"、`--errors retry` 标"与 resolve 相同"、`UID STORE` 说明 ✓

---

## 3. 两处"偏差"的澄清（都不是它的错，但结论要改）

1. **`fake_imap.py` 其实可达** —— OpenCode 说"`//10.0.199.184/.../fake_imap.py` 不可达，按 spec 自行实现"。我实测：

   ```
   $ ls -l "//10.0.199.184/yxo_data/.workbuddy/tmp/idle_probe/fake_imap.py"
   -rw-r--r-- 1 Roke8x 197121 4605 Sep 16 14:34 …
   ```
   ⇒ 资产在。**但它把假服务器内置进测试文件其实更好**（自包含、不依赖 UNC/网络），**建议保留现有做法**，只是"不可达"这个理由不成立。

2. **"基线 372 vs 237"是我 spec 的错**（OpenCode 提出疑问并未擅自改，处理正确）。真相是**口径混用**：
   - `mailbots_next/tests/`（本批的范围）：**247 collected**（我实测）
   - 仓库根全量：**409 collected**（= `mailbots_next/tests/` + `mailbots/tests/`）
   - 而且**算术是闭合的**：我今天 11:19 自己实测 `mailbots_next/tests/` = **236**（+`test_26` = 237）→ OpenCode 的改动 = 旧拓扑 4→3（**−1**）+ 新增 11 ⇒ **237 − 1 + 11 = 247** ✓ 与实测 collected 完全一致。
   ⇒ 结论：**本批的基线是 237，不是 372**；我已在 spec §4.5 把绝对值改成**相对判据 + 采样命令 + 口径说明**（并标注此错由我认领）。

---

## 4. 观察（**均非阻塞**，供洋决定是否顺手改）

1. ⚠️ **静默 continue（建议补一行日志）**：`ingest.py:436-440`，`UID FETCH` 返回非 `OK` 或解析出的 `raw_bytes` 为空时直接 `continue` —— **不推进水位、不记日志、无 counter**。若某封邮件的 payload 永远取不到，会**每 30 秒空转一次且日志无痕**（不是漏发，是"看不见的空转"，与本次事故同族）。建议：`_log.warning` + `increment_counter("uid_fetch_empty")`。
2. **幂等检查少了一处**：`process_email` 的 `IDENTIFY_EXCEPTION` 分支（`serve.py:93-99`）**未做** `has_pending_message(message_id)` 检查，而同一 `stage="identify"` 的 `UNCLASSIFIED` 分支（`:110-115`）做了。同封邮件若在多账号/多文件夹各处理一次，可能**重复入队**。低危（不影响转发正确性），建议补齐对称。
3. **一条断言无意义**：`test_uidvalidity_change_rescans_with_since:309` 的 `assert "08-SEP-2026" not in crit` —— 断言某个具体日期字符串**不出现**什么也证明不了。好在该用例前半句 `"SINCE" in crit` 已经**真锁住**最高危点（若改成 `UID 1:*` 就会挂）。建议把它换成本来想说的 `assert "UID 1:*" not in crit`。
4. **`IDLE_GROUPS` / `get_idle_groups` 保留了 IDLE 味**：spec §1.8 的改名清单只列了 `Idler`/`idle_processors`/`_idlers`（约束1 的 grep 已归零），而 `IDLE_GROUPS` 语义是"**文件夹分组**"、与 IDLE 协议无关。**我判定可接受**（改动面大、零行为收益），但需洋知晓。
5. **小冗余**：`_poll_loop` 里 `_select_folder()` 与 `_process_new_messages()` 开头各调一次 `EXAMINE`（每文件夹每轮 2 次）。无害。
6. **文档过时一处**：`AGENTS.md:293` 仍写"否则 **IDLE** 抓空"，IDLE 删除后应改为"轮询抓空"——建议随本批一起改（属收信层文档配套）。

---

## 5. 待办（按优先级）

| # | 事项 | 归属 |
|---|---|---|
| 1 | **决定 `trustme` 怎么办**（方案 A / B / C），改完我才好把"全绿"作为验收结论 | **洋拍板** → OpenCode 改 |
| 2 | 观察 1/2 是否顺手修（都属"让静默失效可见"） | 洋拍板 |
| 3 | ~~`AGENTS.md:293` IDLE 字样、以及本次 **§5.1 grep 口径**（生产代码 vs 全仓）写回 spec~~ ✅ **已做**（9-17 晚；AGENTS.md 实为 `:293`/`:294` **两行**都过时，一并改正） | 芙蕾雅 |
| 4 | 本批 **单独 commit**（与转发层那批分开），仍**不 push** 等洋发话 | 洋拍板后我执行 |

---

## 6. 整改复验（2026-09-18 早）—— ✅ **三项整改全部通过，阻塞项解除**

> OpenCode 于 9-17 晚完成三项整改（未 commit / 未 push）。以下**全部为我自己的实跑与实读**。
> **结论：⚠️ 有条件通过 → ✅ 通过。** 唯一遗留是一条**非阻塞的环境卫生建议**（见 6.5）。

### 6.1 项一 `trustme`（方案 A）

| 核对点 | 我的实测 |
|---|---|
| `requirements-dev.txt` 声明 | ✅ `requirements-dev.txt:5` `trustme>=1.0  # …（测试专用，勿进生产 requirements）` |
| 生产依赖未被污染 | ✅ `requirements.txt` / `mailbots_next/requirements.txt` 均无 `trustme` |
| 有无 `skip`/`importorskip`/`xfail` 绕过 | ✅ `test_poll_uid.py` 内 **0 处** |
| 全量套件 | ✅ **`249 passed`（EXIT=0）** —— 与汇报一致；且 = 247 + 2 新用例，**算术闭合** |
| 真链路用例 | ✅ `test_true_link_smoke_tls_proves_callback PASSED` |

### 6.2 🔴 但复验当场发现：`trustme` 装进了**另一个解释器**（已处置）

OpenCode 报告"装进的是 `D:\python.exe`"。我实测：

```
我的 pytest 解释器 : C:\Users\Roke8x\.workbuddy\binaries\python\versions\3.13.12\python.exe (3.13.14)
                    → import trustme ⇒ ModuleNotFoundError        ← 用我的解释器跑，仍是 1 failed
OpenCode 的解释器   : D:\python.exe (3.13.13)
                    → import trustme ⇒ 有                          ← 它的 249 passed 在这个解释器上成立
```

⇒ **"依赖已声明"这一半做对了，但"装进跑测试的那个解释器"这一半在另一个解释器上落地** —— 与上一轮 "只在作者机器上绿" 是**同一个病的另一种形态**（这次是"作者的**解释器**和别人不一样"）。

**我的处置**：把 `trustme>=1.0` 装进**真正跑 pytest 的那个解释器**（`python -m pip install "trustme>=1.0"` → 装入 `cryptography 50.0.1` 等 5 个包 → `import trustme` 复核通过），之后全量 **249 passed / 0 failed / EXIT=0**。

> 说明：`requirements-dev.txt` 已经声明了它，所以**新环境按 `pip install -r requirements-dev.txt` 走就是对的**（耐久修复已到位）；这一条不是代码缺陷，属**环境卫生**（见 6.5）。

### 6.3 项二 观察①（FETCH 失败留痕）

实读 `ingest.py:435-443`：

```python
typ, msg_data = conn.uid("FETCH", str(uid), "(BODY.PEEK[])")
if typ != "OK":
    _log.warning(f"UID FETCH rejected | account={self.account} folder={folder_name} uid={uid} typ={typ}")
    increment_counter("uid_fetch_empty")
    continue
raw_bytes = _extract_raw_from_fetch(msg_data)
if not raw_bytes:
    _log.warning(f"UID FETCH empty payload | account={self.account} folder={folder_name} uid={uid}")
    increment_counter("uid_fetch_empty")
    continue
```

✅ WARN **含 account / folder / uid（+ typ）**；✅ 两处都计数；✅ **水位推进逻辑一字未动**（该分支没有 `_advance_watermark`）。

新用例 `test_uid_fetch_failure_leaves_trace_and_holds_watermark`：情形 A（非 OK）/ 情形 B（空 payload）各断言 **on_raw 0 次 → WARN 命中三要素 → 计数 1→2 → 水位 `last_uid == 0`** ✓（断言强度足够：计数与水位都是真值，不是恒真式）。

### 6.4 项三 观察②（`IDENTIFY_EXCEPTION` 幂等）

实读 `serve.py:91-100`：入队前已补 `if not has_pending_message(message_id):`，**与 `UNCLASSIFIED`（`:110-115`）完全对称** ✓

新用例 `test_identify_exception_second_entry_no_duplicate_enqueue`：第一次**真入队**（用真库 `get_pending_errors` 查到 1 条）→ 第二次同 `message_id` 再进该分支 → 断言 `add_error` **未被调用**、且库里**仍只有 1 条** ✓（不是纯 mock 自证）。

### 6.5 🔴 「真锁」往返验证（我自己做的，不是采信）

把两处整改**临时改回旧行为**（`tmp/patch_revert.py`：删掉两处 WARN+counter、把幂等检查条件恒真化）后跑那两条新用例：

```
2 failed, 11 deselected in 2.08s
FAILED …::test_uid_fetch_failure_leaves_trace_and_holds_watermark
FAILED …::test_identify_exception_second_entry_no_duplicate_enqueue
```

⇒ **两条都是真锁**。随后 `cp` 还原并逐文件 `md5sum` + `diff -q` 复核：**`ingest.py` / `serve.py` 均与备份字节一致**（工作区 = OpenCode 交付态，未被我的验证改动）✓

### 6.6 无回退确认

- 启动日志**复跑仍然正确**（serve.py 被改过，故必查）：`Ingest mode: poll 30s | connections=12 | folders=5 | discovery=uid-watermark`，实起 **12 条** ✓
- grep 对账仍为 0：`conn.idle` / `_idle_once|MAX_IDLE_SEC|IDLE_PER_FOLDER|poll_fallback_secs` / `Idler|idle_processors|_idlers` ✓
- 全量 **249 passed / 0 failed**，上一轮验收的 12 条拓扑、水位线、`UID STORE`/`UID FETCH`、`process_email` 契约、`decide_tracing`、`data/` 自建**用例全绿** ⇒ 未回退 ✓

### 6.7 改动范围核实（mtime）

`ingest.py` / `serve.py` 显示 09-18 08:49（**那是我刚才往返验证还原时重置的 mtime**，md5 已证明内容 = 交付态）；`test_poll_uid.py`、`README.md`（含技术债 **T1** 登记行）、`requirements-dev.txt` 均为 09-17 17:03/16:57（= 整改当时）；`settings.py`/`decide.py`/`routing.py` 仍为 09-17 16:03-16:06（上一轮，本轮未动）⇒ **与"本轮只碰 5 个文件"的自述一致** ✓

### 6.8 遗留（非阻塞 · 环境卫生建议）

**本项目没有声明"跑测试用哪个解释器"**：本机同时存在 `D:\python.exe`（3.13.13）与 WorkBuddy 托管 python（3.13.14），两边都可以跑、结果曾经不同。建议二选一（**待洋拍板**）：

- **A（最轻）**：在 `WORKFLOW.md` §9.4（测试）/ `mailbots_next/README.md` 写明**唯一**的解释器与安装命令（含 `pip install -r requirements-dev.txt`）；
- **B（最彻底）**：给仓库建一个 venv（`python -m venv .venv` + 文档写死 `.venv\Scripts\python.exe`），从此不再有歧义。

*本报告以我自己的实跑与实读为准；与 OpenCode 报告冲突处，以本报告结论为准。*
