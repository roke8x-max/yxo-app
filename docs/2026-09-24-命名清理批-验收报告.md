# 验收报告：命名清理批（草单分类死值 + `WAY_B` tier 改名）

> 验收人：芙蕾雅 ｜ 日期：2026-09-24 ｜ 交付方：OpenCode
> 对应 spec：`docs/2026-09-23-命名清理spec(OpenCode)-草单代号与WAY_B.md`
> 交付形态：**改动全部停在仓库工作区**（未 commit / 未 push / 未建分支），HEAD 仍是 `b829246`
> 验收方式：**不采信交付报告** —— 全部判据由我重跑，真锁由我独立重做（含 1 条交付方没做的探针）

---

## 0. 结论摘要

| 项 | 结论 |
|---|---|
| 动作 1（删死值 + 收敛） | ✅ **通过**（含 spec 自相矛盾处由交付方发现并按判据优先解决 —— 见 §4.1） |
| 动作 2（`StrEnum` 枚举 + tier 改名） | ✅ **通过** |
| 顺带项（`route_row` 死桩） | ✅ **通过**（删的是死桩，断言一条未动） |
| 判据 §2.3 第 2/3/4 条 + §3.3 第 1/4/5/6 条 | ✅ **全部复现**（原始输出见 §2） |
| 真锁 L1–L5（spec 要求的 4 条 + 交付方自加 1 条） | ✅ **5/5 复现**，还原后 md5 全部一致 |
| 全量测试 | ✅ `323 passed in 30.66s`，EXIT=0 |
| 🔴 **我加的第 6 条探针** | ❌ **抓到一处「覆盖回归」**：`decide.py` 的"留人工"早退**现已无任何用例锁住** —— 见 §3 |

**两条待你拍板**（§3 与 §4.2）：① 是否补一条 `decide()` 直调 C2 的用例（**我已预验证它能被违反**，见 §3.3）；② 4 份活文档是否加"更名说明"页眉。

---

## 1. 我独立跑的东西

1. **全量测试**（唯一解释器 `…\3.13.12\python.exe`，先清 `%TEMP%\pytest-of-Roke8x`）：
   `& $PY -m pytest -q -p no:cacheprovider mailbots_next/tests/` → **`323 passed in 30.66s`，EXIT=0**。
   与交付报告的 `323 passed in 30.61s` 一致。
2. **基线算术**：不重跑 HEAD（避免在你要提交时动 `.git`），改为**从 diff 直接闭合** —— 净变化 = −1（删 `test_draft_OTHER_unknown_category_skips`）+1（新增 W 用例）+2（新增枚举两条）= **+2**；`test_p1_coverage.py` / `test_waybill_rejected.py` 无净增删 ⇒ 321 → 323 成立。剔除了"静默删用例凑绿"的可能。
3. **真锁 6 条**（§2.3.4）；每条都是：**独立内存备份 → 探行尾 → 断言命中数 == 1 才写 → `ast.parse()` 自检 → 跑全量 → 还原 → 核 md5**。6 条**全部**还原成功。
4. **判据 grep 全部重跑**（Git Bash，本机）。
5. 逐 hunk 读 diff（代码 7 个文件 + `AGENTS.md` + 4 份文档）。

---

## 2. 判据逐条核对（原始输出）

| # | 判据 | 我的原始输出 | 判定 |
|---|---|---|---|
| §2.3.2 | `grep -rnw 'DRAFT_CATEGORIES' mailbots_next/ --include=*.py` | 空（exit 1） | ✅ |
| §2.3.3a | `grep -rnE 'category\s*=\s*"W"\|"C2",\s*"W"\|"W",\s*"OTHER"' …` | 空（exit 1） | ✅ |
| §2.3.3b | `grep -rn '"W"' …` | 恰 2 处：`core/act.py:206`、`tests/test_forward_layer.py:25`（均 `east_asian_width(ch) in ("W","F")`） | ✅ 与 spec 预期逐字一致 |
| §2.3.4 | `grep -rn 'NON_AUTO_DRAFT_CATEGORIES' …` | **9 行 / 5 文件**：`types.py:88`（定义）、`__init__.py:55,126`、`draft.py:14,40`、`decide.py:4,34`、`serve.py:21,302` | ✅ 见 §4.2 |
| §3.3.1 | `grep -rn 'WAY_B' mailbots_next/ --include=*.py` | 空（exit 1） | ✅ |
| §3.3.4 | `grep -rn 'draft_category ==' mailbots_next/tests/` | **6 行**：A×4（402/924/949/964）、C1（1018）、C2（1039），随全绿套件 | ✅ |
| §3.3.5 | `grep -rnE '\bis\s+["\x27]' mailbots_next/ --include=*.py` | 空（exit 1） | ✅ |
| §3.3.6 | `grep -rnI "3\.10" . --exclude-dir=.git --exclude-dir=tmp --exclude-dir=superpowers --exclude='*命名清理spec*'` | 空（exit 1） | ✅ |
| 附 | **改前基线**（`git show HEAD:mailbots_next/README.md \| grep -n '3\.10'`） | **恰 1 行**（`54:- Python 3.10+`）⇒ 1 → 0 成立 | ✅ |
| 附 | `scripts/deploy/DEPLOYMENT_MANUAL.md:47` | 仍是 `Python 3.11+`，与改后 README 一致 | ✅ |
| 附 | 内联元组残留 `grep -rn '("C2"' …` | 仅 2 处合法：`types.py:88`（定义本身）、`test_p1_coverage.py:626`（测试构造行） | ✅ |

**真锁 L1–L5 复现（我重做，非采信）**

| 锁 | 撤改 | 我的全量汇总行 | 挂的用例 | 还原 md5 |
|---|---|---|---|---|
| L1 | `draft.py` 加回 `elif "运单号" … "W"` | `1 failed, 322 passed` | `test_draft_subject_with_waybill_keyword_no_longer_yields_W` | ✅ 一致 |
| L2 | `NON_AUTO_DRAFT_CATEGORIES = ()` | `2 failed, 321 passed` | `test_draft_C2_stays_manual_no_queue_no_notify`、`test_manual_rows_exempt_from_mark_seen` | ✅ 一致 |
| L3 | `NEW = "new_draft"` | `6 failed, 317 passed` | 值/打印两条 + A 语义 4 条（`…_goes_to_tiers` / `…_stays_A_even_if_ledger_exists` / `…_never_opens_a_db` / `…_extractor_basic`） | ✅ 一致 |
| L4 | tier 改回 `"WAY_B"` | `1 failed, 322 passed` | `test_decide_guard_blocks_forward` | ✅ 一致 |
| L5 | `StrEnum` → `(str, Enum)` | `1 failed, 322 passed` | `test_draft_category_strenum_prints_value`（**值断言仍过 ⇒ 两道闸独立**） | ✅ 一致 |

> 与交付报告的差异：L3 它写"2 failed"、我全量跑是"6 failed" —— 它应是只跑了枚举那两条所在的小范围，**不是分歧**。
> L5 的"值断言仍过、只有打印断言挂"与报告一致，这条**恰好证明了选 `StrEnum` 的实质**（值兼容是 `str` 子类给的，打印正确性才是 `StrEnum` 给的）。

---

## 3. 🔴 我抓到的唯一实质问题：`decide.py` 的"留人工"早退已无覆盖（**覆盖回归**）

### 3.1 事实

spec §2.2⑤ 删 `test_draft_OTHER_unknown_category_skips` 时，我给的理由是：

> 「`("MANUAL","skip")` 这条行为**仍由 C2 的用例覆盖**（`test_core.py:1048-1055` 断言 `log_manual_category` 收到 `"C2"`），所以删掉这条不会造成覆盖缺口。」

**这条理由错了。** 我做了 spec 没要求的第 6 条真锁来实证：

```
L6【我加的探针】中和 decide.py 的 MANUAL 早退（if row.draft_category in NON_AUTO…: → if False:）
  → 323 passed in 30.67s   EXIT=0        ← 全绿
```

**中和 `decide.py:34-35` 后，全量 323 条一条都不挂。** 而删掉的那条用例，恰恰是唯一直接调 `decide()` 并断言 `("MANUAL","skip")` 的用例（`grep -rn '"MANUAL"' mailbots_next/tests/` 现在 = **0 命中**）。

**根因**：C2 那条用例走的是 `process_email` 全链路，而 `serve.py:302` 的早分流在**路由与 `decide()` 之前**就 `return "manual"` 了 ⇒ 它验的是 `serve.py` 那道闸，**根本走不到 `decide.py:34`**。所以：
- `serve.py:302`（留人工早分流）—— 有 L2 锁着 ✅
- `decide.py:34`（同一语义的第二道闸）—— **现在无人锁** ❌

对比：改前这条是被锁住的（被删掉的那条用例）。所以本批的净效果里含**一条覆盖回归**，而我的 spec 把它算成了"无缺口"。

### 3.2 是否还有生产影响

没有。`decide()` 在生产唯一的调用点是 `serve.py:401`，而 `draft_category == "C2"` 的行在 `serve.py:302` 就返回了 ⇒ `decide.py:34` 在今天**是防御性代码，正常流程到不了**。所以：**不是线上缺陷，是"这道防御以后没人看着"**。按 spec 的规矩我**没有自行实现**，按 §2.2⑤ 的要求写进报告请示。

### 3.3 我已经把修法预先验证过（可直接用）

候选补测用例（放在 `TestDraftCategories` 内、`test_draft_subject_with_waybill_keyword_no_longer_yields_W` 之前）：

```python
    def test_draft_C2_manual_via_decide_direct(self):
        """decide.py 的"留人工"早退必须自身被直接锁住：C2 行直调 decide ⇒ ("MANUAL","skip")。

        2026-09-24 补：本批删掉 test_draft_OTHER_unknown_category_skips 后，这条早退一度
        无任何用例覆盖（中和它全量仍 323 全绿，已实测）。C2 是现在唯一会走到这里的值。"""
        row = _tier_row("CQWLJT260810001", "CICU1000001", category="C2")
        recs = [_tier_rec("CQWLJT260810001", "CICU1000001")]
        ok = RoutingResult("太平洋", "m@x.com", ["t@x.com"], [], "full_match", True)
        d = decide(EmailType.DRAFT, row, ok, recs)
        assert (d.tier, d.action) == ("MANUAL", "skip")
```

**我实测的两步**（撤改往返，两次都还原并核过 md5）：

| 步骤 | 结果 |
|---|---|
| ① 只追加该用例（代码不动） | `57 passed in 7.65s`，EXIT=0 ✅ 不误报 |
| ② 追加该用例 + 中和 `decide.py:34` | `1 failed, 56 passed`，唯一挂的正是 `TestDraftCategories.test_draft_C2_manual_via_decide_direct` ✅ **有区分力** |

⇒ 这条用例**既不会空跑、又能被违反**，符合"判据必须能被违反"。补上后全套应为 **324 passed**。

> 为什么不干脆删掉 `decide.py:34-35`？它和 `serve.py:302` 是同一语义的两道闸，且**未来若有人把 `decide()` 用到别处**（例如新增直接调用方），这道闸就是唯一保障。留代码 + 补锁，成本最低、语义最稳。**但这是取舍，请你拍板**（采"补锁"还是"删防御 + 在 README 技术债登记"）。

---

## 4. 对交付方"五、偏差与请示"的逐条答复

### 4.1 `types.py` 注释未照抄 spec 原文 —— ✅ **接受，且这是我的 spec 自相矛盾**

我 spec §2.2② 给的注释建议里**含有 `DRAFT_CATEGORIES` 字面与 `("C2","W","OTHER")` 字面**；而我同一份 spec 的判据 §2.3.2（`-w` 必须 = 0）与 §2.3.3（ERE 必须 = 0、`"W"` 必须恰 2 处）**要求这些字面不存在** —— 照抄必挂，而且挂的是我自己的判据。交付方实测踩中后改写注释、保住三条判据，**处理方式正确**。

我的判断：**这个取舍是对的，方向不该反过来**。理由：把 `W` 写在注释里只是"记一笔"，而这三条判据是**长期防回退的闸**（谁再把 `"W"` 写回代码就会亮红）。而且改写没有丢信息 —— 被删的五个值在 `DraftCategory` 里逐条都有（`NEW=A / UPDATE=B / UPSTREAM_FEEDBACK=C1 / EXTERNAL_REPLY=C2 / OTHER=OTHER`），"W 从来不在其中"也写明了。

> 🟡 **记在我账上**：这是"spec 的处方与 spec 的判据互相打架"，属于我这一批反复强调的同一类病（判据没在真实代码上验过就发出去了）。

### 4.2 `NON_AUTO_DRAFT_CATEGORIES` 是 **9 行** 不是 6 处 —— ✅ **接受"按角色验收"，计数口径由我更正**

实测 9 行 = 定义 1（`types.py:88`）+ 导出 2（`__init__.py:55,126`）+ 三文件各"import 1 + 使用 1"（`draft.py:14,40` / `decide.py:4,34` / `serve.py:21,302`）。

我 spec 的"6 处"是**按角色**数的（定义 / 导出 / 使用），**漏算了 3 行 import** —— 交付方的口径（按行）才是可机械核验的。裁定：

> 判据 §2.3.4 读作：**"该常量只允许出现在 `types.py` 定义 1 处、`__init__.py` 导出 2 处、`draft.py`/`decide.py`/`serve.py` 各一 import + 一使用；不得出现第 6 个文件、不得有内联元组"**。按此行数与角色双满足。计数口径以 **9 行** 为准，我的 spec 表述不准。

### 4.3 交付方报告的"shell grep 差异" —— ⚠️ **不复现；判据本身没问题**

交付方称：本机 `grep -rnE …` 会把 `docs/` 也扫进来、`grep -rn '"W"'` 回空。

我在本仓（Git Bash）**逐字重跑三条命令，全部与 spec 预期完全一致**（见 §2 表格：ERE = 空、`"W"` = 恰 2 处、`WAY_B` = 空）。⇒ 该差异是**它那台/那次调用的引号或路径被工具改写**造成的假象，**不是命令或判据的问题**。

裁定：**判据不改**；下次遇到"shell 与 python 不一致"，请在报告里贴**完整命令行原文**（含引号与路径）而不是摘要 —— 否则没法定位是谁把命令改了。

### 4.4 第三批 spec `L100` 的旧用例名是否同步 —— ❌ **不同步**（由"更名说明"覆盖）

`test_rejected_silent_even_when_unroutable` 现存 3 处文档引用：第三批 spec `:100`（处方）、第三批验收报告 `:81`（观察记录）、命名清理 spec `:31`（**故意**以"原名"身份保留）。用例本体 `test_waybill_rejected.py:93` 的 docstring 也已写明"原名 …"。

不同步的理由：① 本批的声明范围是 **tier 常量名**，不是用例名；② 若只改 `:100` 而留 `:81`，**同一批文档内会出现两种口径**，比不改更乱；③ 改名痕迹在 `test_waybill_rejected.py` 的 docstring 与前置调查 §4 映射表里都在，检索落空的代价低。⇒ 见 §5.2 的页眉声明（把"哪些改、哪些不改"写清楚）。

---

## 5. 两条待拍板事项（✅ 2026-09-24 已拍板：① 方案 A 补锁、交 OpenCode 出微 spec；② 页眉加。落地见 §8）

### 5.1 补一条 `decide()` 直调 C2 的用例（见 §3.3，已预验证）

- **方案 A（我建议）**：按 §3.3 原文补 1 条用例，全套 323 → **324**。**只加测试、不动业务码。**
- 方案 B：删掉 `decide.py:34-35` 这道防御，并在 `mailbots_next/README.md` 技术债登记"留人工只在 serve 层"。
- 交付方式：按惯例**出 1 条微 spec 交 OpenCode**（约 15 行）；或者你说一声"你直接改"，我按预验证过的原文改（**仅测试文件 1 处**）。

### 5.2 4 份活文档是否加"更名说明"页眉

**背景**：spec §3.2④ 让我把这 4 份文档里指向新系统的 `WAY_B` 全改掉，交付方忠实执行了（9/14/3/6 行）。但它们**是带日期的记录**，里面有些是**引文**，例如：

- 第三批 spec §2 的表格标题是「**现状**（我已核到行号）」，现在写的是 `Decision("waybill_rejected", …)`；
- 第三批验收报告写「`_log.info("waybill_rejected skipped by design (no notify) …")`」—— 而 9-23 当天代码打的是 `WAY_B skipped by design`。

⇒ 更名本身没错（否则文档在指一个已不存在的名字），但**我 spec 忘了要求"声明"**：现在这 4 份文档在读者眼里成了"当时就叫这个名字"。**建议各加 1 行页眉**（我来写，4 行，零代码风险）：

```markdown
> ⚠️ 更名说明（2026-09-24 命名清理批）：本文件原写的新系统 tier 名 `WAY_B` 已统一改为
> `waybill_rejected`，文中引用的代码行/日志文案均同步更名 ⇒ 在 9-24 之前的提交里看到的
> 实际字样是 `WAY_B`（映射表见 `docs/2026-09-23-命名清理(tier代号)-前置调查与小spec.md` §4）。
> 用例名与其它代号**未**同步更名。
```

---

## 6. 我确认没做 / 场外事实

- 未 commit、未 push、未建分支、未 stash；**未动 HEAD**（未跑 HEAD 版测试以避让 `.git`）。
- 真锁探针中的改动**全部还原并核过 md5**（3 个文件的 md5 与开工时逐字一致：`types.py=636e5b65…`、`decide.py=d33d4509…`、`draft.py=1fe2d515…`）。
- 工作区最终状态 = 13 个已改文件 + 2 个未跟踪文件，与交付态一致；**本文件是唯一新增**。
- 未跟踪的 2 个 `docs/superpowers/specs/*.md` **非本批产生，来历已查明、不动**（见 §8.3）。
- `action_wayb_rejected_skipped`（计数器键）**未改名** —— spec 未要求，且它有 3 处文档引用（第三批 spec/验收报告）与 1 处测试断言；改名会让那几处判据落空。**建议单独一批**，改时连文档一起同步。
- `docs/2026-09-22-单证驳回缺口与测试耦合-分析与推荐.md`（3 处 `WAY_B`）、`docs/2026-09-23-命名清理(tier代号)-前置调查与小spec.md`（23 处）、本 spec（14 处）**均未动** —— 前两份是历史分析/映射表，本 spec 是更名依据，**保留原样才对**。

---

## 7. 这批留下的教训（我的）

1. **"这条行为由另一条用例覆盖"必须实测，不能靠推理。** 我推"`("MANUAL","skip")` 由 C2 用例覆盖"，实际那道闸在 `serve.py`，`decide.py` 那道根本没被走到。**删测试前要撤改一次、看它挂不挂** —— 这本来就是本仓已有的硬规矩（"判据必须能被违反"），我这次只把它用在**新增**用例上，没用在**删除**判定上。
2. **spec 的"处方正文"与"验收判据"也可能互相打架**（§4.1）：我给的注释原文含判据明令不存在的字面。写处方时要回头看一眼判据。
3. **让旧文档"改名"要配一句"声明"**（§5.2）：不带声明的更名，等于把带日期的记录改成后见之明。

---

## 8. 拍板落地与将来项登记（2026-09-24）

### 8.1 两条拍板

| # | 拍板 | 落地 |
|---|---|---|
| 1 | 补 C2 直调用例 → **方案 A：补锁、不删 `decide.py` 那道防御**；**交付方式 = 微 spec 交 OpenCode**（保持"设计 / 落码 / 验收"三分，不让我自己改） | ✅ 已出 `docs/2026-09-24-命名清理批补丁spec(OpenCode).md`（组 A = 补用例，含逐字原文与真锁要求；组 B = 4 份文档页眉） |
| 2 | 4 份活文档**加**"更名说明"页眉，采用报告 §5.2 的 4 行文案 | ✅ 同上，列为补丁 spec 组 B（每文件 +5 行：1 空行 + 4 行正文） |

> 拍板 1 的理由（记录）：`decide.py:34` 是**第二道闸**，生产今天走不到（`serve.py:302` 先返回），但**将来若有人新增 `decide()` 的直接调用方，它就是唯一保障** —— 删掉等于"用现在的调用路径假设未来"；且**只加测试、不动业务码 = 零风险**，补上后 323 → 324 算术闭合。

### 8.2 登记为将来项（**不阻塞本批**）

| 项 | 内容 | 做的时候必须一起改 |
|---|---|---|
| **F1** | 计数器键 `action_wayb_rejected_skipped` → 与 tier 名一致（如 `action_waybill_rejected_skipped`） | ① `serve.py` 的 `increment_counter` 调用；② `test_waybill_rejected.py` 的 3 处（docstring + `before` + `assert`）；③ `docs/2026-09-23-第三批spec…md:63,104,119`；④ `docs/2026-09-23-第三批…验收报告.md:28,84`。**混在本批会让 diff 变脏，故单独一批**；改名后还要考虑"当日计数会断档"（计数文件按天存，历史计数不受影响） |
| **F2** | 2 个未跟踪设计草稿的去留（见 §8.3） | ✅ **2026-09-24 已拍板：就地入库 + 页眉声明**，见 §8.4 |

### 8.3 场外②：两个未跟踪文件的来历（已查明）

| 文件 | 谁建的 | 状态 |
|---|---|---|
| `docs/superpowers/specs/2026-09-08-ticket-system-design.md`（34,608 B，593 行） | **芙蕾雅**（工单系统设计稿：事业部 ↔ 渝新欧待办协同；`ticket_system/` + `yxo_auth/`，**未落码**） | 从未提交（`git log --all -- <path>` = 空）；`docs/2026-09-14-提交方案与PR切分.md:140/219` **已明确记为"不入库、留工作区"** |
| `docs/superpowers/specs/2026-09-21-permission-rbac-module-design.md`（29,502 B） | **芙蕾雅**（权限与隔离模块 RBAC 设计 **v2.4**，文件内写明"作者：芙蕾雅｜落码：OpenCode｜验收：芙蕾雅"） | 同样从未提交；沿 9-14 的处置 |

- 目录对照：同目录另 2 份（`2026-08-26-mailbots-refactor-design.md`、`2026-09-06-unified-error-handling.md`）**是已跟踪文件**，最后随 `fef85f9`(PR #10) 入库。
- 结论：**不是本批产生的散落垃圾，是两份"给未来子系统"的设计真相源 + 一条 9-14 的既有决定** ⇒ 本批**不动**（与洋的指示一致）。
- ⚠️ **风险提示（供 §8.2 F2 决策参考）**：未跟踪 = 不在任何 commit 里 ⇒ 一次 `git clean -fd`、换机、或重新克隆就会**永久丢失**，而 9-08 那份自称"新子系统的唯一设计真相源"。可选：① 就地入库（与目录内另两份同类文档一致）；② 移到仓库外的设计文档位置（如 `C:\Users\Roke8x\WorkBuddy\…` 或留言箱）；③ 维持现状（默认）。**建议倾向 ① 或 ②，不建议继续裸露在工作区。**

### 8.4 场外②的落地：两份设计稿已加归档页眉、待入库（2026-09-24）

**拍板**：**就地入库 + 页眉声明**（理由：① "唯一真相源"裸露在工作区本身就是风险；② `superpowers/**` 已查明**不是**"禁新增"目录 —— 同目录另 2 份就是已跟踪文件；③ 页眉防"以为已落码"的误读）。

**我直接改的两处**（这两份文件作者是我、且文案由洋逐字给定；不在 OpenCode 改动范围内）：

| 文件 | 改动 | 行数 |
|---|---|---|
| `.../2026-09-08-ticket-system-design.md` | H1 之后插「空行 + 归档声明 2 行 + 元信息更正 1 行 + 空行」；并把抬头「（尚未落码、未 commit）」里的 **「未 commit」删去** | 593 → **597**（+4） |
| `.../2026-09-21-permission-rbac-module-design.md` | H1 之后插「空行 + 归档声明 2 行 + 空行」 | 416 → **419**（+3） |

- 归档声明（**洋给的原文，逐字**）：
  ```markdown
  > ⚠️ 设计稿 · 未落码（2026-09-24 归档）：本文是设计阶段产物，代码尚未实现。
  > 归档目的是保留“设计意图”供将来参考；看到本文不代表相关模块已落码。
  ```
- 🔴 **我多加的第 3 行（**9-08 那份**，请你过目）**：扫陈旧表述时发现该抬头还有两处**不成立的事实**，属"页眉元信息"范围，故一并声明（**正文一个字没改**）：
  ```markdown
  > ⚠️ 元信息更正（2026-09-24）：抬头原写「尚未落码、未 commit」；归档入库后「未 commit」不再成立，已删去（只留「尚未落码」）。另：文中出现的 `dev` 分支已于 2026-09-18 废弃 —— 现状只用 `main`（见 `WORKFLOW.md`）。
  ```
  - 「未 commit」在**入库那一刻就变假** ⇒ 与本批一贯的"文档不得承诺不成立的事"同类，必须一起处理。
  - 「`dev` 分支」是**安全问题**：抬头写"代码基准 @ dev 分支"，而 `dev` 已于 **2026-09-18 废弃（只用 `main`）** ⇒ 将来照此实现的人可能真去推 `dev`。只加声明、**不改决策记录**（正文 §0 的 D12「dev→PR→main」保留原样，由这行声明覆盖）。
  - 若你认为这两条超范围，一句话即可撤掉（只影响这 1 行）。
- 验收：`grep -c "设计稿 · 未落码（2026-09-24 归档）"` 两份各 **1** ✅；行数 +4 / +3 ✅；两份均为 **LF** 工作区（与本批一致）。
- **入库安排**：随本批最终提交，**单独一个 `docs(归档)` commit**（与代码/测试改动分开，便于单独 revert）；排在该补丁批之后、开 PR 之前。
- 另一份同目录文件 `2026-09-06-unified-error-handling.md`（已跟踪）与 `2026-08-26-mailbots-refactor-design.md`（已跟踪）**未动**。

---

## 9. 补丁批验收（组 A 补锁 + 组 B 更名页眉）—— 2026-09-24

> 对应 spec：`docs/2026-09-24-命名清理批补丁spec(OpenCode).md` ｜ 交付态：**未 commit / 未 push / 未建分支**，HEAD 仍 `b829246`
> 验收方式：**全部重跑，不采信交付报告**（含逐字比对、全量测试、真锁独立重做、判据两次对照）

### 9.1 组 A：补 `decide()` 直调 C2 用例 —— ✅ **通过**

| 项 | 我的核对结果 |
|---|---|
| 逐字一致 | ✅ 用 python 把文件里那 11 行与我 spec 原文做**逐行比对**，`逐字一致: True`；前一行恰为空白行、后接空白 + `test_draft_subject_with_waybill_keyword_no_longer_yields_W` |
| 锚点唯一 | ✅ `grep -c` = 1（同 spec 要求） |
| 业务码零改动 | ✅ `decide.py` / `draft.py` / `types.py` 等非测试文件**相对头部批无任何新变化**（`git diff --numstat` 逐个对照头部批数值：`decide.py` 4/4、`types.py` 17/2、`draft.py` 5/6…**全部未变**） |
| 全量测试 | ✅ `324 passed in 30.98s`，EXIT=0（改前 323） |
| 旧用例确已消失 | ✅ `test_draft_OTHER_unknown_category_skips` 在文件中**不存在**（不是被 diff 对齐藏起来） |
| 新增行数口径 | 交付报告写 **+11 行**（用例本体）；实测 **净 +12**（多 1 行 = 与上一条用例之间的分隔空行，符合 PEP8 两空行/一空行的既有风格）。**这不是偏差**，是计数口径差 |

**真锁（我独立重做，非采信）**：中和 `decide.py:34`（连行首 4 空格一起取，`ast.parse()` 自检 OK）

```
开工 md5 = d33d450943a6fbef1cb570cf631b4da9   （与 spec 给定基线一致）
pytest  : 1 failed, 323 passed in 30.71s      EXIT=1
挂的用例: ['test_draft_C2_manual_via_decide_direct']
失败详情: AssertionError: assert ('T1', 'forward') == ('MANUAL', 'skip')   (test_core.py:1067)
还原后 md5 = d33d450943a6fbef1cb570cf631b4da9   一致 ✅
```

> 与交付报告逐项一致（它 30.86s / 我 30.71s）。**失败详情本身值得记下来**：一旦这道闸没了，C2 行会落到 `('T1','forward')` ⇒ **把外部回复类草单转发给客户** —— 这就是这道"生产今天走不到"的防御的真实代价，也印证了拍板 1"补锁不删防御"。
> ⚠️ 我这次给 pytest 加了 `PYTHONDONTWRITEBYTECODE=1`，避免验证动作再往 `__pycache__` 写 `.pyc`（见 §9.3）。

### 9.2 组 B：4 份文档更名页眉 —— ✅ **通过**

| 项 | 我的核对结果 |
|---|---|
| 页眉位置与内容 | ✅ 4 份**逐字一致**，均位于 **:3**（= H1 / 空行 / 4 行页眉 / 空行 / 原 `> 交办`） |
| 只加 5 行、别处未动 | ✅ **决定性证据**：4 份的首个 hunk 均为 `@@ -1,4 +1,9 @@`（+5），**其余所有 hunk 只是行号平移**（`7→7`、`10→10`、`15→15`、`12→12`…大小完全不变）⇒ 除插入的 5 行外**没有任何内容改动** |
| numstat 两次对照 | ✅ 当前实测 `14/9、8/3、19/14、11/6`，与交付报告**逐字相同**；且我**自己此前读过头部批的 `--stat`**（18/6/28/12 = 9+9 / 3+3 / 14+14 / 6+6）⇒ 每文件新增 = 开工值 **+5**、删除**不变**，**我的独立基线与其报告一致** |

### 9.3 🔴 三条判据口径更正（**根因都在我**，不在交付方）

交付方报的"偏差"里有两条，追下去是**我写的判据被我自己的文件/动作污染了**。三条一并更正：

| # | 原判据 | 实测 | 根因 | **更正后的口径** |
|---|---|---|---|---|
| 1 | §1.3.2 `grep -rn '"MANUAL"' mailbots_next/tests/` 改前 = **0**、改后 **恰 1** | 在我 shell 里**按字面成立**（恰 1 行，无 Binary 行） | 交付方 shell **会剥掉 pattern 内的双引号** ⇒ 退化成 `MANUAL`，多命中 docstring 行 + `.pyc` Binary 行；且**改前并非 0**，因为**我自己跑预验证探针时留下了陈旧 `.pyc`**（里面含探针 docstring 的 `("MANUAL","skip")`）—— 交付方的推断**完全正确** | **改成不依赖引号、且跳过二进制**：<br>`grep -rnI 'MANUAL' mailbots_next/tests/` ⇒ 改前 **0** / 改后**恰 2 行**（`:1058` docstring、`:1067` assert）。实测 ✅ |
| 2 | §2.3.1 `grep -rn "更名说明（2026-09-24 命名清理批）" docs/` ⇒ **恰 4 行**，且只在那 4 个文件里 | 原始输出 **8 行** = 目标 4 + **本补丁 spec 3 处**（模板/示例/判据引文）+ **本验收报告 1 处** | **同一份判据的"示例文本"与"判据引文"本身就含该串** —— 正是我在同一份 spec 的 §2.3.2 里特意警告过的"期望值取决于文件自身内容"，**结果我在 §2.3.1 又犯了一次** | 豁免判据自身的文件：<br>`grep -rn "更名说明（2026-09-24 命名清理批）" docs/ --exclude='*补丁spec*' --exclude='*命名清理批-验收报告*'` ⇒ **恰 4 行、均为 :3**。实测 ✅（交付方建议的"目标 4 文件各 1 行即算过"**予以采纳**） |
| 3 | 头部批 §3.3.6 `grep -rnI "3\.10" . --exclude-dir=… --exclude='*命名清理spec*'` ⇒ **0** | 本补丁之后变 **1 行** = **本验收报告 `:50`**（我在表里引用了 `grep -n '3\.10'` 这条基线命令） | 同上：判据的记录文件自身污染 | 把豁免扩到"所有我自己写的命名清理文档"：<br>`… --exclude='*命名清理*'` ⇒ **0 行**。实测 ✅（一条通配即覆盖 `命名清理spec` / `命名清理批-验收报告` / `命名清理批补丁spec`） |

> ⇒ 三条的**同一个教训**（已升为硬规矩）：**写"期望 0 / 期望恰 N"的 grep 判据时，必须把"将来会记录这条判据的文件"和"二进制缓存"都排出扫描范围**；否则判据会在记录它自己的那一刻失效。**这不是交付方的问题**（第 1 条更是我跑预验证污染的 —— 见 §9.4）。

### 9.4 我这次自己踩的两个坑（自认）

1. **预验证探针留下陈旧 `.pyc`，污染了后续判据的基线**：我把"改前 `"MANUAL"` = 0 行"写进 spec 时是真的（当时确为 0），但**随后我自己跑的预验证探针把带 `("MANUAL","skip")` 的 `__pycache__/*.pyc` 留在了工作区** ⇒ 交付方开工时"改前"已不是 0。**根因**：`__pycache__` 虽然被 `.gitignore` 忽略，但 **grep 看得见**。
   ⇒ **新规矩**：① 跑真锁/预验证一律加 `PYTHONDONTWRITEBYTECODE=1`；② 一切 grep 判据加 `-I`（或 `--include='*.py'`）跳二进制。**我的验证动作本身会改变被验证环境** —— 这跟"mtime 不可信"是同一类事。
2. **判据的记录文件污染判据**（§9.3 第 2、3 条）。
3. ✅ 附带确认：交付方删掉的那个 `.pyc` **不是 tracked 文件**（`git check-ignore` → `.gitignore:3 __pycache__/`）⇒ **不产生任何仓库改动**，属正当的卫生处理。

### 9.5 偏差裁决汇总

| 交付方请示 | 裁决 |
|---|---|
| §2.3.1"只出现在 4 个文件里"字面不成立 | ✅ **采纳其建议**：口径 = **目标 4 文件各 1 行（均在 :3）**，其余 4 行是我文件里的判据自指，已由 `--exclude` 口径排除（§9.3 第 2 条） |
| §1.3.2 在其 shell 下是 3 行、源码实质 1 行，请确认接受 python 口径 | ✅ **接受**，但判据**改用不依赖引号的写法**（`grep -rnI 'MANUAL'` = 2 行），从根上消掉"谁的 shell 剥不剥引号"这个变量，避免下次再对不上 |
| 开工清掉一个陈旧 `.pyc` | ✅ **正当且必要**；已确认非 tracked、不产生仓库改动；`.pyc` 来源是我自己的预验证探针（§9.4） |

### 9.6 补丁批结论

**组 A ✅ + 组 B ✅ + 主体批判据全部回归通过**（本次重跑：`DRAFT_CATEGORIES -w` = 0、ERE = 0、NON_AUTO = 9 行、`draft_category ==` = 6 行、`is` = 0、`WAY_B`(mailbots_next) = 0、`"W"` = 2、`3.10` = 0（更正口径））。

⇒ **代码侧可以收尾**：全部改动（主体批 + 补丁批 + 2 份设计稿归档）停在同一个工作区，未 commit。下一步 = 建分支 → 按功能切分提交 → push → 开 PR（等你确认后执行）。
