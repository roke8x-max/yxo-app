# 第三批验收报告：waybill_rejected 改静默 + 通知收件人去重

> 验收人：芙蕾雅 ｜ 日期：2026-09-23 ｜ 交付方：OpenCode
> spec：`docs/2026-09-23-第三批spec-WAY_B改静默与通知去重(OpenCode).md`
> **本报告以我自己的实跑与实读为准；与交付报告冲突处，以本报告结论为准。**

## 0. 结论

✅ **通过，可以提交。** 两组改动与 spec 逐字一致；**我自己重建了全部 4 条真锁**（不是复读它的报告）；独立探针验证去重行为正确；全仓 483 passed。

---

## 1. 基线与指纹（先核这个，防在未知改动上做验收）

| 项 | 实测 |
|---|---|
| 分支 / HEAD | `docs-and-cleanup-20260922` / `942d34f`（未动）✓ |
| 三文件 md5 | `serve.py=1c406e91`、`decide.py=58da8e32`、`notify.py=1920591f` — **与交付报告逐一相符** ✓ |
| 用例数 | `mailbots_next/tests/` = **321** = 318（第二批）+ 1（⑦计数）+ 2（去重）✓ 算术闭合 |
| 本轮 delta | 恰 **5 个文件**（`serve.py` / `decide.py` / `notify.py` / `test_notify_collapse.py` / `test_notify_email.py`）✓ |
| 未动确认 | `core/act.py` **未改**（精确匹配核过；`extract.py` 名字里含 `act.py` 会误判，已用 `-x` 全行匹配排除）✓；`waybill.py` md5 仍是第二批的 `44cd51d8` ⇒ 本批未碰 ✓ |

---

## 2. 逐组核对（实读 diff）

**A 组（waybill_rejected 改静默）**
- `serve.py:308-317` 早分流已经是静默版：`_log.info("waybill_rejected skipped by design (no notify) …")` + `increment_counter("action_wayb_rejected_skipped")` + `return "rejected"`，**位置仍在 `route_row` 之前**（这条是上一批真正的技术目标，没被顺手改回去）✓
- `decide.py:83-87` 守卫保留、动作 `notify_rejected → skip`，注释里"否则会命中 T1 转发给客户"的警告保住了 ✓
- `notify.py` 的 `send_waybill_rejected` 整段已删；全仓 `send_waybill_rejected` / `notify_rejected` = **0 命中** ✓
- `serve.py:201-207`（标已读门控）**未动** ✓；`waybill_rejected` 字段 / 关键词 / 正文取码分支**全部保留** ✓

**B 组（去重）**：`notify.py:236` 恰好 1 行改动 `for recipient in dict.fromkeys(recipients)` ✓

**连带（1 行）**：`test_notify_collapse.py` 的 `test_non_collapsible_types_never_collapse` 断言 `6+6+6+3 → 3+3+3+3`。核实**成立且必要**：该用例的文件级 `ADMIN = "maoxiaoyang@cqtransit.com"` 与 `OPS_OWNER_EMAIL` **确为同一地址**（我实核过；探针也证实），去重后每次 notify 只发 1 次 ⇒ 不改就必红。注释里已写明原因 ✓

---

## 3. 我自己重建的 4 条真锁（每文件独立备份 / 缩进纳入匹配 / `ast.parse()` 自检 / 还原核 md5）

| 撤改 | 实测结果 | 还原 md5 |
|---|---|---|
| **A1** 删 `serve.py` 早分流整块 | **3 failed, 4 passed**（挂 ②④⑦，附 `Routing failed: no_route` —— 正是要根治的噪音）| `1c406e91` ✅ |
| **A2** 删 `decide.py` 守卫（直接委托 `decide_draft`） | **1 failed**（仅挂 ⑤ ⇒ 守卫是独立第二道闸）| `58da8e32` ✅ |
| **A3** 终态词 `"rejected" → "manual"` | **1 failed**（挂 ⑦）| `1c406e91` ✅ |
| **B1** 去重改回 `recipients` | **2 failed, 1 passed**（挂 `test_duplicate_recipients_send_once` + 连带那条）| `1920591f` ✅ |

⇒ **锁与实现点一一对应，无"只测一处"的漏洞**；4 次撤改均 `ast.parse()` 先行（无语法错误）；还原后三文件 md5 与交付值**逐一相同**。

> **它申报的 A1 还原事故我核过**：`serve.py` 现在是**单个干净 hunk**（`+11`），无错位残留，`_build_send_ctx` 零改动 ⇒ 后果已完全清除。

---

## 4. 独立探针（不复用它的测试写法，直接驱动 `_send_now`）

```
① 同一人两次           → 1 次 ['毛骁洋']                    ✓
② 两个不同人           → 2 次 ['毛骁洋','冯茜']              ✓（防"去重成只发第一个"）
③ [B,A,B,A,B] 保序去重 → 2 次 ['冯茜','毛骁洋']              ✓（dict.fromkeys 保首次出现顺序）
④ 负责同事 == OPS      → 1 次（修复前为 2 次 = 两条相同消息） ✓ ← 这条就是线上真会发生的场景
⑤ 重复的未知收件人     → 0 次发送，notify_failed 记 1（不是 2）✓（侧收益）
```

---

## 5. 对它请示那条的裁决：**`"rejected"` 是对的，不用改**

它问："`live` 下 `"manual"` 会导致不标已读（与旧系统一致的做法相悖），方向请确认。"

**我的裁决：确认 `"rejected"` 正确。** 依据（实读 + 生产旧系统对照）：
- `serve.py:181` 只在 `row_outcome == "manual"` 时置 `has_manual`，而 `:205` 的标已读门控是 `config.is_live() and uid and not has_manual and not has_pending_message(...)`；
- 返回 `"rejected"` ⇒ **不置 `has_manual`** ⇒ **照旧标已读** ⇒ 与生产旧系统一致（旧系统 `Waybill_Robot.py:995` 的 `mark_seen_everywhere` 在 A/B 两类**共用尾部**）；
- 这正是 ⑦ 那条加强断言（`outcome["rejected"] == 1`）要锁的行为，**我独立复验该锁成立**。

⇒ 它把"锁不住"如实上报、并**用加强断言让锁真正生效**（而不是静默跳过），做法正确。

---

## 6. 我抓到的 3 处小问题（**都不阻塞**）

1. 🟡 **用例④ 的 `route_row` mock 已成"死桩"**：早分流在路由之前 ⇒ `route_row` 永不调用 ⇒ `test_rejected_silent_even_when_unroutable` 的 patch 在**交付态下不生效**，两条断言变成平凡为真。
   - 它仍**不是空测试**：我的 A1 撤改（删早分流）时它会挂 ⇒ 有区分力；
   - 但名字/说明与实际不符（"even when unroutable"的前提已不存在）。**建议下次顺手改名**（如 `test_rejected_silent_regardless_of_routing`）或删掉那个 patch，**本批不动**。
2. 🟡 **我的 spec 判据 3 写得不准**：我写"`action_wayb_rejected_skipped` 测试内 **1 次**"，实际是 **3 处**（docstring + `before` + `assert`，同一用例内）。**这是我的 spec 不精确，不是它的偏差** —— 它在报告里如实申报了。
3. 🟡 **计数会进每日汇总（FYI，非缺陷）**：`send_digest` 会 `sorted(counts.items())` 渲染**所有**计数 ⇒ 每天那条摘要里会出现 `action_wayb_rejected_skipped: N`。
   - 我认为这**正是"静默 ≠ 没痕迹"想要的**（每天一次、不刷屏），与其他计数口径一致；
   - 若你不想在摘要里看到它，说一声 ⇒ 1 行改动（不计数、只留 info 日志）。

---

## 7. 全量测试（我自己跑的）

```
mailbots_next/tests/   → 321 passed in 30.82s   EXIT=0
整个仓库               → 483 passed, 4 warnings in 40.34s   EXIT=0
                         483 = 321（新系统）+ 162（旧系统）  ← 算术闭合
```
4 个 warning 是旧系统 `mailbots/tests/unit` 的 Windows 文件锁，既有、与本批无关。

---

## 8. 无静默删除

`git diff` 里删除行只有 3 处：`notify.py` 的 1 行（去重替换）、`test_notify_collapse.py` 的 1 行注释+断言（连带）、以及随 `send_waybill_rejected` 整段删除的行（spec 明令）。**除 spec 要求的删除外，无任何断言被弱化或删除**。

---

*报告完毕。本批与第二批均未提交，提交方式待洋决定。*
