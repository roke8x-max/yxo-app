# 上线前收尾改动（A/B/D/E 四组）· 验收报告

> 验收人：芙蕾雅 ｜ 日期：2026-09-20 ｜ 交付方：OpenCode
> 分支：`chore-launch-cleanup-abde`（HEAD = `65920a3` = 远端 main）｜ **未 commit / 未 push**
> 对应 spec：`docs/2026-09-18-上线前收尾改动spec(OpenCode).md`（A/B/D/E 四组 + C 组独立批次）
> **结论：✅ 通过**（另附 1 处文档不一致【已由我修】+ 2 条非阻塞建议）

---

## 1. 基线与范围（只信 `ls-remote`）

| 项 | 实测 |
|---|---|
| 分支名 | `chore-launch-cleanup-abde` —— **扁平名** ✓（本仓建不了 `fix/xxx` 这类嵌套名） |
| HEAD | `65920a3` —— **正好等于远端 `main` 尖端** ✓ ⇒ 从 main 拉、无多余提交 |
| 远端 | `main = 65920a3`、`dev = d26e7f3`（已废弃，未动）✓ |
| 提交状态 | **未 commit** ✓（12 个 M + 3 个新测试 + 1 个新脚本，全在工作区） |
| 改动量 | 代码 11 文件 **+263 / -103**（不含我的文档）＋ 4 个新文件 727 行 |

## 2. 测试结果（我自己跑的，不采信汇报）

**口径先对账，再算增量**（铁律：不写会漂移的绝对值，用相对判据）：

```
mailbots_next/tests/  →  278 collected  =  我 9-18 的基线 249  +  新增 29（A10 + B4 + D12 + E3）
整个仓库              →  440 collected  =  278 + 162（旧 mailbots/ 测试）
```

⇒ **增量恰好等于声称的新增用例数，分毫不差** —— **没有"删旧用例凑绿"**。

**执行结果（唯一解释器 `…\3.13.12\python.exe`，默认 basetemp）**：

```
440 passed, 3 warnings in 39.56s     EXIT=0
```

与交付报告一致。3 条 warning 是旧 `mailbots/` 测试的 Windows 文件锁，与本批无关。

## 3. 逐组实核

### A 组 · 公司收件人初始化 —— ✅（含 3 项实跑）

- **dry-run 打真实生产库副本**（`\\10.0.199.184\…\yxo.db` 只读副本）：**8 家全对**，to/cc 数量与我 9-18 手工核对的生产库**完全一致** ——
  东盟 3/1、中欧木业 1/3、保时达 2/1、同程配 1/1、太平洋 2/1、沙坪坝 1/2、港九港铁 1/1、**联运 4/1**；输出脱敏（`h***@cail56.com`）✓
- **实跑 `--apply` 两次** ⇒ **幂等**（第二次 8 家全跳过、写入 0）；其间**手工改掉「沙坪坝」**为 `MANUAL@example.com`，再跑 ⇒ **人工值完好未被覆盖** ✓（company 行数仍 8）
- **源库不可读时** ⇒ 打印错误 → 「共 0 家」→「源端无数据，未写入任何行」→ **EXIT=2、0 行写入** ✓
  > 说明：我**原本怀疑**这里会"静默只灌联运 1 家"（因为 `companies["联运"]=…` 写在函数尾部）。实跑后**证明是我多虑** —— 两条失败路径的 `return {}` 都发生在联运注入之前。**记下来以免下次重复怀疑。**
- `check_company_recipients_configured()` 只读（无任何写）、0 行 → ERROR、由 `main()` 启动时调用一次 ✓
- 脚本无 `source` 列、`ON CONFLICT DO NOTHING` / `--force` 打印脱敏旧值、退出码语义 ✓

### B 组 · 企微 loud fail —— ✅（含实跑）

**实跑失败路径**（本机确无 `wecombot` 包 ⇒ 天然的"客户端不可用"场景）：

```
notify() = False        # 不抛异常
send_program_error() = False
notify_failed: 1 -> 3   # 精确 +2
ERROR 日志含脱敏收件人：to=['ma***g@cqtransit.com']
```

- 客户端不可用：启动期 **ERROR 一次**（模块 flag 防刷屏）✓
- 四条失败路径（client-None / 单收件人 ok=False / 无映射 / 异常）**都**计数 + ERROR ✓
- 返回值语义不变（bool）、**不重试、不抛**（不阻断转发主链路）✓；`_fallback_owner_email` 未动 ✓
- 收件人掩码委托 `log` 模块，**全仓同一口径** ✓

### C 组 —— ✅ 未动（符合"等 WeComBot 端点就绪后独立批次"）

### D 组 · 连接复用 + T5 七项 —— ✅（本组最高危，逐条核）

- `_poll_loop`：`conn = None` 在循环外、惰性连接、**轮次间复用** ✓；`abort / error / OSError / socket.timeout` → `WARN "connection lost, reconnecting"` + `increment_counter("poll_reconnect")` + `_drop` + 重连 ✓ —— **不会被当成"这轮没有新邮件"吞掉**
- **`socket` 已 import（`:7`）**：我实跑 `import` 确认模块真的绑定了 `socket`（若没绑，异常元组本身会抛 `NameError`）✓
- **我 spec 补的两条约束都被采纳、还写进了 docstring**：
  - `_run_round`：「每轮必 select（复用连接也不跳过）：UIDVALIDITY 从 untagged_responses 读取，跳过 select 会拿到陈旧值 ⇒ 该重扫时不扫（漏信）」✓
  - `_process_new_messages`：「前置条件：调用方已 select，且之后未执行其它 IMAP 命令 —— `_get_uidvalidity` 必须紧随本次 select 读取」✓；函数体第一句就是 `_get_uidvalidity(conn)` ✓
- **异常传播结构正确**：`:526 except Exception: pass` 是内层（notify 调用）的；`:528 → raise` 属外层、**连接异常确实向上传播**（我按缩进逐层核过嵌套，不是同层 `except`）✓
- **不得回退项全在**：致命异常 → 幂等兜底入队 → 才推进水位（`:495-510`）✓；`historical_skip` 推水位 ✓；`uid_fetch_empty` 的 WARN+计数 ✓；`FORWARD_SINCE` 门禁重扫（非 `UID 1:*`）✓
- T5：`--once`（顺序单轮即退，且**在启动 batcher/sweeper 之前就 return**）✓；`IngestManager` 删除（含 `core/__init__` 导出）✓；`get_idle_groups → get_folder_groups`（无别名，`__all__` 同步）✓；`_last_outcome → threading.local`（sweep 侧同步改）✓；水位**内存 + 轮末一次落盘**（`_flush_watermark` 无 dirty 不碰文件）✓；#6 已按洋的决定**登记不改**（新 T2 行）✓
- **启动日志一字不差**：`Started 12 ingest pollers (poll 30s)`、`Ingest mode: poll 30s | connections=12 | folders=5 | discovery=uid-watermark`，实际起停 **12 条** ✓

### E 组 · `import_excel.py` 守卫 —— ✅（真锁由我独立验）

- SQL 与 spec **逐字一致**：`AND COALESCE("状态",'')<>'退舱' AND COALESCE(is_deleted,0)=0 ORDER BY id LIMIT 1` ✓
- `analyze_import`（纯预览计数）确实未共用该 SQL、未动 ✓
- **真锁（我自己做的往返验证）**：把守卫撤掉、回到旧行为 ⇒ `test_retired_and_deleted_rows_untouched_plus_insert` **必挂（1 failed）**；还原 ⇒ **md5 与备份一致**、`3 passed` ✓

## 4. 既有测试被改的 5 处 —— 评估：**全部正当，且有一处变强**

| 改动 | 判定 |
|---|---|
| 真链路用例加 `p._select_folder(conn, "F1")` 前置 ×1 | ✅ 正当 —— T5-1 把 select 责任上移到调用方，测试改为与生产同序（`_run_round` 也是先 select 再 process） |
| 3 处读水位文件前加 `_flush_watermark()` | ✅ 正当 —— T5-7 改为轮末落盘，读文件前必须 flush；断言仍是"文件里 last_uid == 期望值"（若 advance 没被调用，flush 会早退、文件缺键 ⇒ 用例照样挂，**没有变弱**） |
| `test_data_dir_autocreate`：先 `assert not exists()` → flush → `assert exists()` | ✅ **反而变强** —— 现在把"延迟写"语义双向锁住了 |

⇒ **不存在"改断言迁就实现"**。

## 5. 交付方自述的偏差 —— 我逐条核了

| 它的说法 | 我的核实 |
|---|---|
| spec 引用的 `ingest.py:567` 实际在 `:524` | ✅ 属实 —— 我引的是改动前的行号，语义一致 |
| `BASE_FIELDS` 不含箱号，断言改用目的站 | ✅ 属实 —— `import_excel` 只刷 `BASE_FIELDS` 的 7 个字段 |
| B 组在"单收件人 ok=False"也加了计数 | ✅ 属扩展、口径一致（"发不出去"都记） |
| A 脚本生产默认路径未实跑 | ✅ 我已用**只读副本**补跑 dry-run + apply（见 §3-A）；生产 `--apply` 仍应由小叽在服务器执行 |

## 6. 我发现的问题

### 🔴 1. `README:170` 的 `--once` 说明与实现不符 —— **已由我修正**

原文仍写着「`--once` 参数已解析但未实现（单轮扫描后退出）：**请勿使用**」，而本轮已实现（T5-2）。
⇒ 我已改为：**已实现**（顺序跑一轮即退出、不起线程/循环/sweeper/digest），并**补了两个语义要点**：
① 它**不启动标已读 batcher** ⇒ 跑完**不会把邮件标为已读**（作为"无副作用自检"是有意的）；
② 退出码 0 **不代表 SMTP 真发出去了**（仍看日志与 `forward_log`）。

### 🟡 2. `notify.py` 调用了 `_log._mask_recipients`（**私有方法**）

掩码"单一实现"的意图是对的，但跨模块依赖私有 API 属脆弱点：`EmailLogger` 一旦改名/重构，**loud fail 路径本身会抛 `AttributeError`**（那就从"静默失败"变成"崩在告警上"）。
⇒ 建议（**非阻塞、下批做**）：把掩码函数提到公共位置（或加一个公开别名并在测试里锁住）。

### 🟢 3. A 脚本的边界：源库"可读但 company 行数为 0"时会只灌「联运」1 家

这不是缺陷（`联运` 本来就该灌），但**摘要容易看漏**（只显示"共 1 家"）。建议（可选）：这种情形额外打一句 WARN 提醒"源端 0 行，只灌了联运"。

## 7. 结论与下一步

**✅ 通过** —— A / B / D / E 四组均按 spec 落地，**真锁经我独立往返验证成立**，不得回退项全部保留，未 commit / 未 push 符合铁律。

**待洋决定**：
1. 本批**按 A / B / D / E 四组分别 commit**（文件天然分离；`README.md` 的改动建议并入 D 组 commit，因为它是 D 的 T5-6/T5-2 产物）；
2. 我修正的 `README:170` 一并纳入（属文档，随 D 组或单独一个 docs commit 均可）；
3. §6 的第 2、3 条建议**是否要现在做**（我的建议：不阻塞上线，登记后再排）。

**C 组**仍等小叽的 `/api/notify` 端点；就绪后我把 C 组 spec 交 OpenCode。

## 8. 补充验收（同日）：§6 两条建议已落地 + 外部评审五条复核

### 8.1 落地的两处（洋授权"一起改是对的"）

**(1) 脱敏函数提升为模块级公共实现（修 §6 第 2 条）**

| 位置 | 改动 |
|---|---|
| `mailbots_next/core/log.py` | 新增 `mask_email()` / `mask_recipients()`（**唯一实现**，模块级）；内部 5 处调用由 `self._mask_*` 改调模块级；`EmailLogger._mask_email` / `_mask_recipients` 改为**兼容委托**（⚠️ **不能删**：`test_p1_coverage.py:451-454` 直接断言方法名） |
| `mailbots_next/core/notify.py` | `_mask_addrs()` 由 `_log._mask_recipients(...)` 改调 `mask_recipients(...)`；import 行同步 |

**为何不删私有方法**：既有用例直接引用它们，删了会挂测试。真正的修复点是"跨模块不再依赖私有 API"，而不是"消灭同名方法"。

**实跑证据**：
- 行为等价：`maoxiaoyang@cqtransit.com → ma***g@cqtransit.com`、`ab@x.com → a*@x.com`、`no-at-sign → 原样`，委托结果与公共函数一致 ✓（与 3 条既有断言逐条对上）；
- 🔴 **真锁（模拟改名）**：运行时 `delattr(EmailLogger, "_mask_recipients")` + `delattr(..., "_mask_email")` 后，`_mask_addrs(["maoxiaoyang@cqtransit.com"])` **仍返回 `['ma***g@cqtransit.com']`** ✓ —— 改回旧写法（走私有方法）时该用例**必挂**，已实测。

**(2) A 脚本补"源库可读但 0 行"的显式 WARN（修 §6 第 3 条）**

`scripts/seed_company_recipients.py`：在 `rows = ...fetchall()` 之后、`companies["联运"] = ...` 之前插入 WARN。

**为何只有"0 行"这条路径踩坑**：不可读时 `return {}` 发生在联运注入**之前** ⇒ 提前退出（EXIT=2、0 行写入），是安全的；而"可读但 `scope='company'` 0 行"会正常走完流程并注入联运 ⇒ **静默只灌 1 家**，且 `if not companies and dry_run` 那句提示永不触发（此时 `companies` 至少有联运）。修复后实跑输出：

```
[种子] ⚠️ 源库可读，但 bot_config 中 scope='company' 为 0 行 —— 本次只有常量注入的
「联运」1 家会被写入，其余公司收件人为空、相关邮件会判 no_route。请先核对源库路径与查询口径是否正确。
[dry-run] 联运 to=4 cc=1 ...
```

### 8.2 新增用例 2 条（含真锁往返）

| 用例 | 文件 | 真锁验证 |
|---|---|---|
| `test_mask_addrs_survives_logger_private_api_rename` | `test_notify_email.py::TestWeComLoudFail` | 改回 `_log._mask_recipients(...)` ⇒ **FAILED** |
| `test_zero_source_rows_warns_before_injecting_lianyun` | `test_seed_company_recipients.py` | 删掉 WARN 整块 ⇒ **FAILED** |

还原后 `md5` 一致（`notify.py=4b6bdbb8`、`seed_company_recipients.py=9cdb503f`）；全量 **442 passed**（`440 + 2`），EXIT=0。

### 8.3 外部评审五条 —— 我逐条实核

| # | 它说的 | 我的核实结论 |
|---|---|---|
| 🔴 | D 组 `poll_secs` 缺上限校验，配置过大 ⇒ **漏信** | **事实对、后果不准、且漏了第二条入口**。① `settings.py:54-58` 确只有 `<=0` 下限、无上限，README 也只写默认 30 ⇒ **事实成立**；② 但 `_poll_loop` 的异常分支是 `_drop` → `sleep(10)` → **回到循环顶重连并重跑本轮**（不是跳过），且水位只在"成功 / 已入队"时推进 ⇒ 服务端断开的结果是**延迟约一个轮询周期**，**不是漏信**；③ **它没提的第二条入口**：`serve.py:812` 的 `--poll-secs` 会**绕过** `settings.py` 里加的任何校验 ⇒ 上限必须卡在两条入口**合流之后**（`serve.py:865-874`）。其"阿里云 IMAP 空闲超时通常 30 分钟"属外部经验值，**我无法从代码核实**，故 1500 这个取值需洋/小叽确认 |
| 🟡 | B 组 ERROR 日志在连续故障时会刷屏 | **成立**，登记备查（计数仍在、现象仍在，不阻塞上线） |
| 🟡 | E 组 `analyze_import` 预览与执行不一致（说更新 X 行、实际 X−1） | **不成立**。`analyze_import` 只统计 `{班列号: 票数}`（供散舱/专列预判，`app.py:1385`），**不做行数预览**；"新增 N 行 / 更新 M 行"是 `run_import` 执行时的真实累加（`import_excel.py:271`）⇒ 不存在"预览 vs 实际"的比对对象。**不登记** |
| 🟢 | 两条小改各自并入对应组；commit 顺序 A→B→E→D | **同意**（私有 API → B 组、A 脚本 WARN → A 组、顺序按简单到复杂） |
| 🟢 | `README.md` 全并入 D 组 | **建议改为单独第 5 个 commit**。理由：本轮 README 含**B 组**内容（已知限制 1 改 loud fail）与 D 组内容（T2 行、`--once`）⇒ 并入 D 会产出"B 组 commit 里没有 B 的文档""D 组 commit 里夹着 B 的内容"；单独一个 `docs:` commit 语义更纯、revert 更干净、成本为 0 |

---

## 9. D 组补丁「轮询周期上限 1500 秒」验收 —— ✅ 通过

**基线**：HEAD `7afb6ec`（上一批 docs commit）；工作区仅有本批 4 个文件（+ 9-18 遗留的 `AGENTS.md`/`WORKFLOW.md` 未提交）。

| 项 | 我的实测 |
|---|---|
| 用例口径 | **448 collected = 442 + 6**（与交付方一致 ⇒ 未删旧用例凑绿） |
| 全量 | **448 passed / 4 warnings**（我自己跑的） |
| 代码 | 与 spec 逐字一致：常量 `serve.py:478`、函数 `:481`、两处调用 `:889`（`--once`）/ `:899`（主循环）；`max(1, …)` 语义未变 |
| `settings.py` | **只 +2 行注释、逻辑与取值零改动**（diff 逐行核对） |
| **独立探针**（我自己写的，未复用它测试的写法） | **8/8 符合预期**：`env=3600`→1500、`--poll-secs 3600`→1500、**1500 边界不夹**、**1501 越界 1 秒即夹 + WARN**、`60`→60、默认→30、**`-5`→30（回默认）**、`--once --poll-secs 3600`→1500（WARN 中 `source` 正确标为 `--once`） |
| **真锁（比它的更细）** | 撤 `--once` 处夹取 ⇒ **只挂 `test_default_and_once_obey_clamp`**；撤主循环处 ⇒ **只挂 `test_env_entry_clamped` + `test_cli_entry_clamped`** ⇒ **用例与两个调用点一一对应**，不存在"只测了一处"的漏洞。还原后 `serve.py` md5 `651ae5e7…` 与改前一致 |
| grep 验收 | `serve.py` 命中常量 + 定义 + **恰好 2 处调用**；`mailbots_next/config/` 内**无定义、无调用**，唯一命中是 §2.4 要求的那两行注释 ✓ |

**它的"偏差"申报：正确，且是应当鼓励的行为。** 我 spec 的 §5 判据第 3 条要求 `grep POLL_SECS_MAX mailbots_next/config/` **字面 0 命中**，而 §2.4 又要求追加含该词的两行注释 —— **这是 spec 自身的自相矛盾**。它**没有擅自删注释**，而是如实申报并请示。

**处置**：① **保留注释**（它的价值正是防止将来有人"顺手在 settings 里加上限"，那会造成"env 生效、CLI 不生效"的隐性不一致）；② **修正 spec 判据**（§5 第 3 条已改为"不得有定义或调用，允许注释提及"，并注明修正原因）。⇒ **本条不是交付方的问题。**

---

*本报告以我自己的实跑与实读为准；与交付报告冲突处，以本报告结论为准。*
