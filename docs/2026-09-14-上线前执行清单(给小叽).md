# 上线前执行清单（给小叽）

> 日期：2026-09-14 ｜ 编写：芙蕾雅（给洋 review，由小叽在生产执行）
> 目标机器：`10.0.199.184`，仓库 `D:\YXO_DATA\yxo_app`
> 配套文档（在同一个仓库 `docs\` 下）：
> - `2026-09-09-联运改造-生产部署执行单.md`（**数据灌入脚本在这里，§2 必跑**）
> - `2026-09-14-上线前必办spec(OpenCode)-写库路径与记账时序.md`（环境变量与加固的来由）

---

## 总原则（先看这 4 条）

1. **任何写操作前先备份**：`yxo.db`、`mailbots_next\data\bot_config.db`。
2. **有疑问就问洋，不要自己决定**。本清单之外的改动一律不做。
3. **切换只做一件事的翻转**：停旧 → 起新。中间不要顺手做别的（比如不要顺便开退信监控）。
4. **每一步都有"期望看到什么"**。对不上就停下、贴现场信息给洋，**不要"再试一次"硬推**。

---

## 一、这次上线到底在干什么（大白话）

旧的邮件机器人是**几个独立脚本**（草单/ATB、DSK、运单号），靠 `start_dsk.bat` + `start_waybill_loop.bat` + 计划任务跑起来。
新的 `mailbots_next` 是**一个统一服务**，把这些活接过去。

所以上线 = **把旧的停掉、把新的起起来**。另外有两件"铺路"的事必须先干：

- **数据**：新系统的"公司→收件人"路由表（`bot_config.db`）**不会自己生成**，得按执行单灌进去。**不灌 → 邮件找不到收件人 → 不转发**。
- **环境变量**：三个变量必须核对（第二节）。其中 `YXO_DB_PATH` 不设会踩网络路径写库的坑；`FORWARD_SINCE` 不设**新服务会直接拒绝启动**。

**退信监控（NDR）这次不开**，代码默认就是关的，只要不设那个变量就行。

### 1.1 联运的收件人与抄送人（先记住这 5 个地址）

**"联运"这家公司转发邮件时，实际发给谁 —— 真值如下：**

| 类型 | 地址 |
|---|---|
| **收件人 to（4 个）** | `gongqilin@cqjzxly.cn`（龚琪琳）<br>`yuyanling@cqjzxly.cn`（余燕伶）<br>`jjb@cqjzxly.cn`（蒋佳宝）<br>`wanglu@cqjzxly.cn`（王露） |
| **抄送 cc（1 个）** | `3841559246@qq.com`（洋的测试/监控邮箱，也当公司公共抄送） |

**真值的唯一运行时来源**是生产库 `mailbots_next\data\bot_config.db`（`bot='all', scope='company', key='联运'`）。仓库里另有**两份文档**写了同一组值，可交叉核对：

- `docs\2026-09-09-联运改造-数据灌入执行单.md`（灌入清单 + 脚本常量 `LIANYUN_TO` / `LIANYUN_CC`）
- `docs\2026-09-09-联运改造-生产部署执行单.md` §0

**随时自查命令**（任何时候想知道"某公司发给谁"，就用这条）：

```powershell
cd D:\YXO_DATA\yxo_app
.\venv\Scripts\python.exe -c "import sys; sys.path.insert(0,'.'); from mailbots_next.core.store import get_recipients; print(get_recipients('联运'))"
```

✅ 期望输出：

```
(['gongqilin@cqjzxly.cn', 'yuyanling@cqjzxly.cn', 'jjb@cqjzxly.cn', 'wanglu@cqjzxly.cn'], ['3841559246@qq.com'])
```

（把 `'联运'` 换成 `'保时达'` 之类，就能查其它公司。）

> ⚠️ **两个"看起来像答案、其实不是"的地方**（读代码时最容易踩）：
>
> 1. `mailbots_next\core\store.py` 的 `seed_owner_mapping()` 里有 `"联运": "fengqian@cqtransit.com"`。
>    这是**内部负责人（owner）** —— 只决定**企微通知发给谁**，**不是邮件收件人**。联运的邮件**不会**发给冯茜。
> 2. 测试文件里出现的 `lx_to@test.com` 是**占位夹具**，不是真值。
>
> **一句话记住**：想知道"某公司的邮件发给谁"，**查库（用上面这条命令），不要看代码** —— 新系统的 company 收件人是生产数据，**不在仓库里**，只有 `owner`（内部负责人）才写在代码里。

---

## 二、环境变量核对（3 项，必须先做）

用 PowerShell（管理员）。设完**新开一个窗口**再核对。

### 2.1 必设：`YXO_DB_PATH` → 指向本地盘

**为什么**：新系统会**回写** `yxo.db`（DSK/ATB 时间戳、运踪记录）。这个变量的**代码默认值是网络路径** `\\10.0.199.184\yxo_data\yxo_app\data\yxo.db`。写库走 SMB、且会启用 SQLite 的 WAL 模式，而 **SQLite 官方明确 WAL 不支持网络文件系统**，典型报错是 `attempt to write a readonly database`。更麻烦的是写库失败会被误判成"发信失败"，导致**同一封邮件重发给客户**。

机器人就跑在这台机器上，所以用本地盘路径指向同一份库即可。

```powershell
[Environment]::SetEnvironmentVariable('YXO_DB_PATH','D:\YXO_DATA\yxo_app\data\yxo.db','Machine')
```

核对（**新开窗口**执行）：

```powershell
[Environment]::GetEnvironmentVariable('YXO_DB_PATH','Machine')
```

- ✅ 期望看到：`D:\YXO_DATA\yxo_app\data\yxo.db`
- ❌ 如果回显是 `\\10.0.199.184\...`，或**一片空白** → 没设成功。**空白等于没设**，运行时会落到代码里的 UNC 默认值。重做。

> 若新服务是用 nssm 起的，把 `YXO_DB_PATH=D:\YXO_DATA\yxo_app\data\yxo.db` 加到该服务的环境变量里（`nssm set <服务名> AppEnvironmentExtra ...`），别只设 Machine 级——两种方式选一种，别漏。

### 2.2 必设：`FORWARD_SINCE` → 上线当天日期

**为什么**：这是**防呆门禁**。新系统第一次以 live 模式启动时，如果不设这个，它会把**邮箱里所有历史未读邮件**当成新邮件，**批量转发给外部客户**——这是事故级的。所以不设的话，服务会**直接拒绝启动**并提示你设。

```powershell
[Environment]::SetEnvironmentVariable('FORWARD_SINCE','2026-09-15','Machine')
```

⚠️ 把 `2026-09-15` 换成**实际切换那一天**（格式 `YYYY-MM-DD`）。填错成很早的日期 = 门禁形同虚设。

核对：

```powershell
[Environment]::GetEnvironmentVariable('FORWARD_SINCE','Machine')
```
- ✅ 期望：`2026-09-15`（实际切换日）
- ❌ 空白 → 新服务起不来，日志会提示 `Refusing to start in live mode without FORWARD_SINCE`

### 2.3 必不设：`BOUNCE_MONITOR_ENABLED`

**为什么**：退信监控本轮不上线（数科部退信邮箱/别名还没定，关联键也还没实测）。代码默认关闭，**只要不设这个变量就是关的**。

```powershell
[Environment]::GetEnvironmentVariable('BOUNCE_MONITOR_ENABLED','Machine')
```
- ✅ 期望：**空白**
- ❌ 若是 `1` → 删掉再重启：
  ```powershell
  [Environment]::SetEnvironmentVariable('BOUNCE_MONITOR_ENABLED',$null,'Machine')
  ```

---

## 三、代码拉到最新

```powershell
cd D:\YXO_DATA\yxo_app
git status --short          # 期望：干净（无修改文件）
git pull --ff-only origin main
git log --oneline -1
```

- ✅ 期望：`git log` 顶行是洋合并 PR 后的 main 提交；`git status` 干净。
- ❌ `git status` 有本地修改 → **先别 pull**，把输出贴给洋（可能有人手改过生产代码）。
- ❌ `git pull` 报冲突/非快进 → 停下贴给洋，**不要 `git reset --hard`**。

---

## 四、装依赖（在新服务的 venv 里）

新系统需要 3 个包：`beautifulsoup4`（HTML 正文）、`openpyxl`（.xlsx）、`xlrd`（.xls）。

```powershell
cd D:\YXO_DATA\yxo_app
.\venv\Scripts\python.exe -m pip install -r mailbots_next\requirements.txt
```

- ✅ 期望末行出现 `Successfully installed ...`（或 `Requirement already satisfied`）。
- ❌ 卡在下载 → 加国内源重试：
  ```powershell
  .\venv\Scripts\python.exe -m pip install -r mailbots_next\requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```
- 核对：
  ```powershell
  .\venv\Scripts\python.exe -c "import bs4, openpyxl, xlrd; print('deps ok')"
  ```
  ✅ 期望输出 `deps ok`

---

## 五、灌数据（收件人路由）—— 按执行单 §2

> **这一步不做，新系统转发时找不到收件人。是硬前置。**

涉及**写库**，所以：

1. **先备份**（两个库都备）：
   ```powershell
   $ts = Get-Date -Format 'yyyyMMdd_HHmmss'
   Copy-Item D:\YXO_DATA\yxo_app\data\yxo.db "D:\YXO_DATA\backup\yxo_$ts.db"
   Copy-Item D:\YXO_DATA\yxo_app\mailbots_next\data\bot_config.db "D:\YXO_DATA\backup\bot_config_$ts.db"
   ```
   （`D:\YXO_DATA\backup` 不存在就先建目录。备份完**确认两个文件大小非 0** 再继续。）

2. **执行灌入脚本**：见 `docs\2026-09-09-联运改造-生产部署执行单.md` 的 **§2**（新系统 `bot_config.db`），照抄那段 Python。
   - 目标：8 家 company 行（7 家现有 + **联运**）全部以 `bot='all', scope='company'` 写入。
   - **不要手写 `CREATE TABLE`** —— 脚本里 `import mailbots_next.core.store as store` 会建正确的表结构。

3. **验收**：
   ```powershell
   cd D:\YXO_DATA\yxo_app
   .\venv\Scripts\python.exe -c "import sys; sys.path.insert(0,'.'); from mailbots_next.core.store import get_recipients; print('联运:', get_recipients('联运'))"
   ```
   - ✅ 期望：`联运: (['gongqilin@cqjzxly.cn','yuyanling@cqjzxly.cn','jjb@cqjzxly.cn','wanglu@cqjzxly.cn'], ['3841559246@qq.com'])`
   - ❌ 返回空列表 → 没写进去，回执行单核对 `to_addrs` 必须是 **JSON 数组文本**（`["a@x","b@x"]`），不是逗号串。

---

## 六、先干跑一遍（test 模式，不发信）

**强烈建议**：正式切换前先用 test 模式扫一遍，看有没有导入错误、企微连接问题。

```powershell
cd D:\YXO_DATA\yxo_app
.\venv\Scripts\python.exe -m mailbots_next.serve --once
```

> 注意：**必须在仓库根 `D:\YXO_DATA\yxo_app` 下跑**，**不要** `cd` 进 `mailbots_next\`。并且是 `-m mailbots_next.serve`（模块方式），不是直接跑文件。

- ✅ 期望日志：`Starting MailBot Next in TEST mode`，无 `Traceback`。
- 日志位置：`D:\YXO_DATA\yxo_app\mailbots_next\logs\mailbot_<日期>.log`
- ❌ 出现 `ModuleNotFoundError` / `ImportError` → 贴日志给洋（多半是依赖没装全或启动目录不对）。

---

## 七、切换：停旧 → 起新

> ⚠️ **顺序不能反**：必须停掉旧的，否则新旧同时转发 = 客户会收到重复邮件。

### 7.1 停旧系统

```powershell
# ① 看现在有哪些 pythonw 在跑（旧系统是 pythonw 起的）
Get-Process pythonw -ErrorAction SilentlyContinue | Select-Object Id,StartTime,Path

# ② 禁用计划任务（旧系统每 15 分钟拉起一次运单号机器人）
Disable-ScheduledTask -TaskName 'YXO_WaybillRobot_Start'

# ③ 停进程 —— ⚠️ 把 <Id> 换成上面 ① 列出来的真实 Id，别原样复制
Stop-Process -Id <Id>
```

再确认一次（应该**没有**输出）：

```powershell
Get-Process pythonw -ErrorAction SilentlyContinue | Select-Object Id,Path
```

- ❌ 还有 pythonw → 说明没停干净，**先别起新服务**，贴输出给洋。
- 提示：旧系统由 `start_dsk.bat`（拉草单/ATB/草单转发）和 `start_waybill_loop.bat`（运单号）启动。如果你现场是用别的方式（比如已经注册成服务）起的，就按对应方式停。

### 7.2 起新系统（live 模式）

```powershell
cd D:\YXO_DATA\yxo_app
.\venv\Scripts\python.exe -m mailbots_next.serve --live
```

**先手动前台跑一次**，确认一切正常，再考虑常驻方式。

- ✅ 期望日志（逐条核对，见第八节）：`Starting MailBot Next in LIVE mode`
- ❌ 报 `Refusing to start in live mode without FORWARD_SINCE` → 回 2.2，变量没设上（设完要新开窗口）。
- ❌ 报 `attempt to write a readonly database` → 回 2.1，`YXO_DB_PATH` 没生效（还是网络路径）。

**常驻方式（二选一，按你现场情况）**：

- **手动**：用任务计划程序配"开机启动"，或一直挂一个窗口（简单但不抗重启）。
- **nssm 服务**：⚠️ **不要沿用旧的 `YXO-MailBot` 服务参数**——那套是给**旧目录结构**写的（`PYTHONPATH` 指向旧 app 目录），新系统要的是"在仓库根、以模块方式运行"。若要用服务，请新建一个，工作目录设 `D:\YXO_DATA\yxo_app`，命令 `D:\YXO_DATA\yxo_app\venv\Scripts\python.exe -m mailbots_next.serve --live`，并把第二节的三个环境变量配进服务。**配之前先问洋**。

---

## 八、启动自检（复制这几条，逐条确认）

```powershell
$log = Get-ChildItem D:\YXO_DATA\yxo_app\mailbots_next\logs\*.log | Sort-Object LastWriteTime -Desc | Select-Object -First 1
"=== 日志文件: $($log.FullName) ==="

Write-Host "`n[1] 启动模式（期望见到 LIVE mode）:" -ForegroundColor Cyan
Select-String -Path $log.FullName -Pattern 'Starting MailBot Next in' | Select-Object -Last 1

Write-Host "`n[2] 企微是否可用（期望 0 条）:" -ForegroundColor Cyan
Select-String -Path $log.FullName -Pattern 'WeCom client not available' | Measure-Object | Select-Object -ExpandProperty Count

Write-Host "`n[3] 是否仍在用网络路径（期望 0 条）:" -ForegroundColor Cyan
Select-String -Path $log.FullName -Pattern 'UNC|网络路径' | Measure-Object | Select-Object -ExpandProperty Count

Write-Host "`n[4] 有无异常（期望 0 条）:" -ForegroundColor Cyan
Select-String -Path $log.FullName -Pattern 'Traceback' | Measure-Object | Select-Object -ExpandProperty Count
```

| 项 | 期望 | 不对怎么办 |
|---|---|---|
| [1] | 有 `LIVE mode` | 没起来 → 看上面报错 |
| [2] | **0 条** | >0 = 企微连不上 → **贴给洋**（已知坑，别自己改代码） |
| [3] | **0 条** | >0 = `YXO_DB_PATH` 仍没生效 → 回 2.1 |
| [4] | **0 条** | 有 Traceback → 贴完整堆栈给洋 |

> 关于 [2]：如果企微发不出去，**邮件转发仍然正常工作**，但同事收不到企微通知。不阻塞上线，但必须报给洋。

---

## 九、端到端验收（发一封真邮件）

1. **造一封测试邮件**：走正常的草单/运单号流程（内容带**联运**标识，让路由命中"联运"）。
2. **看收件人**：确认这 4 个都收到 —— `gongqilin@cqjzxly.cn`、`yuyanling@cqjzxly.cn`、`jjb@cqjzxly.cn`、`wanglu@cqjzxly.cn`，并且 `3841559246@qq.com` 在**抄送**里。
3. **看企微**：冯茜（联运 + 保时达负责人）应收到企微通知。
4. **看写库**：`yxo.db` 里该邮件的 DSK/ATB 时间戳或运踪记录应有写入（可与切换前的记录方式对比）。
5. ❌ 收件人不全 / 一个都没收到 → 先查第五节验收命令是否返回了 4 个地址，再贴日志给洋。

---

## 十、上线后 24 小时观察

每天 **09:00** 会发一条**汇总消息**（digest）给洋的邮箱。重点看两点：

1. **有没有一条醒目的中文告警**（形如 ⚠️ 开头，说"发信后记账失败 N 次"）。
   - 有 → 意思是：**邮件已经正常发给客户了，只是本地没记上账，不会重发**；但对应的 DSK/ATB 时间戳或运踪记录可能缺失，需要人工核对补齐。**把这条转给洋。**
   - 没有 → 正常。
2. **看有没有异常计数**（如果某天某类错误计数明显偏高，报给洋）。

另外随时可查"失败重试队列"：

```powershell
cd D:\YXO_DATA\yxo_app
.\venv\Scripts\python.exe -m mailbots_next.serve --errors list
```

- ✅ 期望：`Pending: 0, Manual: 0`
- ❌ 有 Pending → 通常是临时故障（会自动重试，最多 3 次）；有 **Manual** → 需要人工看，**贴给洋**。

---

## 十一、本次**不做**的事（避免多做多错）

| 项 | 说明 |
|---|---|
| **退信监控（NDR）** | 保持关闭（2.3）。等洋那边确认"退信里能不能带回关联标识"之后再开，届时只需设 `BOUNCE_MONITOR_ENABLED=1`，其它默认即可 |
| **WeComBot（:5001）** | 本次**不动**，不重启、不更新 |
| **旧的 `mailbots\` 目录** | 本次**不删**。它还要留着做参照；删它要等新系统跑稳、洋单独安排 |
| **客户端口 8765 回调** | 已知限制（生产环境收不到"确认N"回调），本次不处理 |

---

## 十二、回滚（万一不对劲）

```powershell
# ① 停新服务（前台跑的按 Ctrl+C；挂了服务/计划任务的先停掉）
#    若已注册成服务：
# Stop-Service <新服务名>

# ② 确认没有残留的新服务进程
Get-Process pythonw,python -ErrorAction SilentlyContinue | Select-Object Id,Path

# ③ 恢复旧系统
Enable-ScheduledTask -TaskName 'YXO_WaybillRobot_Start'
#    然后双击 D:\YXO_DATA\MailBots\start_dsk.bat 和 start_waybill_loop.bat
```

回滚后**务必确认旧系统转发恢复正常**（发一封测试邮件走一遍）。数据不用回滚（新系统只多做记账，不会破坏原数据）。

---

## 十三、出问题找谁 / 要准备什么现场信息

**找洋。** 报问题时请一次给全这几样，能省一轮来回：

1. 卡在哪一步（第几节）
2. 完整命令 + **完整报错/日志原文**（不要只截图半行，不要只描述"报错了"）
3. 日志文件：`D:\YXO_DATA\yxo_app\mailbots_next\logs\mailbot_<日期>.log`
4. 当前代码版本：`cd D:\YXO_DATA\yxo_app; git log --oneline -1`
5. 三个环境变量的回显（第二节的三条核对命令的输出）

> ⚠️ **不要**为了"先跑通"而：改代码、跳过 `FORWARD_SINCE`、把 `YXO_DB_PATH` 设成网络路径、或者开 `BOUNCE_MONITOR_ENABLED`。任何一条都会把"小问题"变成"给客户发错邮件"。
