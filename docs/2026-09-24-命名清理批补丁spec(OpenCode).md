# 命名清理批 · 补丁 spec（OpenCode）：补 C2 直调用例 + 4 份文档更名声明

> 交办：芙蕾雅 ｜ 日期：2026-09-24 ｜ 执行：OpenCode ｜ 验收：芙蕾雅
> 前置：`docs/2026-09-24-命名清理批-验收报告.md`（本批主体已验收通过：`323 passed`、判据全绿、5 条真锁复现）
> **洋 2026-09-24 拍板两条**：① 补 C2 直调用例走**方案 A（补锁，不删 `decide.py` 的那道防御）**，**交付方式 = 出微 spec 交 OpenCode**（不让我自己改，保持"设计 / 落码 / 验收"三分）；② 4 份活文档**加**"更名说明"页眉。
> **开工基线**：主体批的改动**已在工作区**（未 commit）。本补丁**在它之上继续改**，**不要回退主体批任何内容**。HEAD 仍是 `b829246`。

---

## 0. 交付纪律（四条铁律，先读）

1. **不瞎改已做好的内容** —— 本补丁只改下面点到的位置；别的"顺手能改的"写进报告不要动。
2. **不要 commit、不要 push、不要建分支**。改完**停在工作区**。
3. **做完先自测**：跑测试 + 真锁往返，**原始输出贴进报告**。
4. **如实报告**：偏差、不确定处、失败都写出来；**不要替人拍板**。

---

## 1. 组 A：补一条"`decide()` 直调 C2"用例（**只加测试，不动业务码**）

### 1.1 为什么要补（我已实证，不是推理）

主体批删掉了 `test_draft_OTHER_unknown_category_skips`，我当时的理由是「`("MANUAL","skip")` 这条行为仍由 C2 用例覆盖」——**这条推理是错的**：

- C2 那条用例（`test_core.py` 的 `test_draft_C2_stays_manual_no_queue_no_notify`）走的是 `process_email` 全链路，而 **`serve.py:302` 的留人工早分流在路由与 `decide()` 之前就 `return "manual"`** ⇒ 它验的是 `serve.py` 那道闸，**根本走不到 `decide.py:34`**。
- 实测：把 `decide.py:34` 的判定**中和掉**（`if row.draft_category in NON_AUTO_DRAFT_CATEGORIES:` → `if False:`），**全量 323 条一条都不挂**（EXIT=0）。
- `grep -rn '"MANUAL"' mailbots_next/tests/` 现在 = **0 行**（该行为已无任何字符串痕迹）。

⇒ 结论：`serve.py:302` 有 L2 真锁着 ✅；`decide.py:34-35`（同语义的第二道闸）**现在无人锁** ❌。
**洋拍板：补锁，不删防御** —— 生产今天走不到它（`decide()` 唯一调用点在早分流之后），但**将来若有人新增 `decide()` 的直接调用方，这道闸就是唯一保障**；删掉等于"用现在的调用路径假设未来"。

### 1.2 改动：在 `mailbots_next/tests/test_core.py` 的 `class TestDraftCategories` 内**逐字**新增下面这条用例

插入锚点：**紧接在**下面这一行**之前**（该行在文件中唯一，我已核过）：

```
    def test_draft_subject_with_waybill_keyword_no_longer_yields_W(self):
```

要插入的原文（**逐字采用，不要改措辞、不要改缩进；docstring 里的引号是中文引号，照抄**）：

```python
    def test_draft_C2_manual_via_decide_direct(self):
        """decide.py 的"留人工"早退必须自身被直接锁住：C2 行直调 decide ⇒ tier=MANUAL、action=skip。

        2026-09-24 补：上一批删掉 test_draft_OTHER_unknown_category_skips 后，这条早退一度
        无任何用例覆盖（把该分支中和掉，全量 323 条仍全绿 —— 已实测）。C2 是现在唯一
        会走到这里的值；这条不是空跑，中和该分支时它必挂。"""
        row = _tier_row("CQWLJT260810001", "CICU1000001", category="C2")
        recs = [_tier_rec("CQWLJT260810001", "CICU1000001")]
        ok = RoutingResult("太平洋", "m@x.com", ["t@x.com"], [], "full_match", True)
        d = decide(EmailType.DRAFT, row, ok, recs)
        assert (d.tier, d.action) == ("MANUAL", "skip")
```

（`_tier_row` / `_tier_rec` / `RoutingResult` / `decide` / `EmailType` 都是该文件里已有的名字，不需要新增 import。）

### 1.3 组 A 验收判据（**改前基线我已实测，写在括号里；这些数不是估的**）

1. 全量：`& $PY -m pytest -q -p no:cacheprovider mailbots_next/tests/` ⇒ **`324 passed`**、EXIT=0（改前 = 323）。
2. `grep -rn '"MANUAL"' mailbots_next/tests/` ⇒ **改前 = 0 行** → **改后恰 1 行**（就是新用例的 `assert`）。
   > ⚠️ 判据写的是**恰 1 行**，所以如果你改了 docstring 措辞（例如把 `tier=MANUAL、action=skip` 写成带引号的 `"MANUAL"`），计数会变 —— **改了就要在报告里报出新计数**，别硬凑。
3. 除 `mailbots_next/tests/test_core.py` 外，**不得有任何其它文件被改动**（`git status --short` 逐行贴出来）。

### 1.4 组 A 真锁（**必做，1 条**：撤改 → pytest 原始输出 → 还原 → md5）

- **撤改**：把 `mailbots_next/core/decide.py:34` 的
  `    if row.draft_category in NON_AUTO_DRAFT_CATEGORIES:`
  改成 `    if False:`（**连行首 4 个空格一起取**；改完先 `ast.parse()` 自检再跑测试）。
- **期望**：**恰 1 条挂** —— `TestDraftCategories::test_draft_C2_manual_via_decide_direct`。
- **还原基线**：`mailbots_next/core/decide.py` 的 md5 必须是 **`d33d450943a6fbef1cb570cf631b4da9`**（我给的开工值；还原后请核一遍，不一致就说明没还原干净）。
- 顺带说明：**这一条我已经预验证过**（① 只加用例 = 57 passed 不误报；② 加用例 + 中和 = 唯一挂的就是它）。所以你跑出来应当是同样的结果；**如果你跑出来不是这样，那就是环境或写法有差异，如实报告，不要改判据**。

---

## 2. 组 B：4 份活文档加"更名说明"页眉

### 2.1 为什么要加

主体批按 spec §3.2④ 把这 4 份文档里指向新系统的 `WAY_B` 全改成了 `waybill_rejected`（9 / 14 / 3 / 6 行），**改得对**（否则文档在指一个已不存在的名字）。但这 4 份是**带日期的记录**，其中有些是**引文**：

- 第三批 spec §2 的表格标题是「**现状**（我已核到行号）」，现在写的是 `Decision("waybill_rejected", …)`；
- 第三批验收报告写「`_log.info("waybill_rejected skipped by design (no notify) …")`」，而 9-23 当天代码打的是 `WAY_B skipped by design`。

⇒ 不带声明的更名 = **把带日期的记录改成后见之明**。所以各配一句声明。

### 2.2 改动：在下面 4 个文件的 **H1 标题行之后**，插入「一个空行 + 下面 4 行」

4 个文件（路径逐字，注意文件名里的 `WAY_B` **不要改**）：

1. `docs/2026-09-22-第二批spec-WAY_B与测试基础设施(OpenCode).md`
2. `docs/2026-09-23-第三批spec-WAY_B改静默与通知去重(OpenCode).md`
3. `docs/2026-09-23-第三批WAY_B改静默与去重-验收报告.md`
4. `docs/2026-09-23-第二批WAY_B与测试基础设施-验收报告.md`

插入的 4 行（**逐字采用，一个字都不要改**）：

```
> ⚠️ 更名说明（2026-09-24 命名清理批）：本文件原写的新系统 tier 名 `WAY_B` 已统一改为
> `waybill_rejected`，文中引用的代码行/日志文案均同步更名 ⇒ 在 9-24 之前的提交里看到的
> 实际字样是 `WAY_B`（映射表见 `docs/2026-09-23-命名清理(tier代号)-前置调查与小spec.md` §4）。
> 用例名与其它代号**未**同步更名。
```

改完后每个文件的开头应长这样（**原有的 `> 交办：…` 那些行一动不动**，只多了一段）：

```
# <原标题>

> ⚠️ 更名说明（2026-09-24 命名清理批）：…
> …
> …
> …

> 交办：…（原有内容）
```

### 2.3 组 B 验收判据（**改前基线我已实测**）

1. `grep -rn "更名说明（2026-09-24 命名清理批）" docs/` ⇒ **改前 = 0 行** → **改后恰 4 行**，且**只出现在上面那 4 个文件里**。
2. **每个文件恰好 +5 行**（1 个空行 + 4 行正文），且**没有动到其它任何内容**。
   ⚠️ 这 4 个文件相对 HEAD 已经是"改过的"（主体批改了 `WAY_B`），所以**不要**拿"相对 HEAD 的 +N"当判据（那会随主体批的行数漂移）。**用相对判据**：
   - **开工第一步**先记：`git diff --numstat -- <上面 4 个文件的路径>`
   - **改完再记一次**同样命令
   - ⇒ 每个文件的**新增行数必须 = 开工值 + 5**，删除行数**不变**。两次原始输出都贴进报告。
3. `git diff` 逐个文件目视确认：增量**就是那 5 行**，位置在 H1 之后。

---

## 3. 本补丁**不做**的事（写清楚免得顺手）

- ❌ 不动 `decide.py` 的任何逻辑（方案 A = **只补锁**；不要删那道防御）。
- ❌ 不改 `docs/2026-09-23-命名清理spec(OpenCode)-草单代号与WAY_B.md`、`docs/2026-09-23-命名清理(tier代号)-前置调查与小spec.md`、`docs/2026-09-22-单证驳回缺口与测试耦合-分析与推荐.md` —— 它们的 `WAY_B` 是**故意保留**的（分别是更名依据、映射表、历史分析）。
- ❌ 不改 `test_rejected_silent_even_when_unroutable` 在文档里的引用（第三批 spec `:100` / 第三批验收报告 `:81`）—— 洋已拍板**不同步**，且上面的页眉声明里已写明"用例名未同步更名"。
- ❌ 不改计数器键 `action_wayb_rejected_skipped`（洋已拍板**单独一批**再做）。
- ❌ 不动 `mailbots/`（老系统）、`docs/superpowers/**`、那两个未跟踪文件（见 §5）。
- ❌ 不改这 4 个文档的文件名。

---

## 4. 报告格式

```
一、改动文件（组 A / 组 B 分别列：文件 + 改了什么 + 行数）
二、测试（323 → 324 的汇总行原文 + 退出码；被改用例与新用例逐条列名）
三、真锁往返（组 A 那 1 条：撤改动作 → pytest 原始输出 → 还原后 md5）
四、判据（§1.3 第 2/3 条 + §2.3 第 1/2/3 条，贴原始输出；组 B 要贴两次 numstat）
五、偏差与请示
六、不确定 / 我没做的事
```

---

## 5. 背景（供理解，不必回应）

- **两个未跟踪设计稿的事已拍板、且已由芙蕾雅处理完（不在你的改动范围内）**：`docs/superpowers/specs/2026-09-08-ticket-system-design.md`（工单系统设计，芙蕾雅稿）与 `2026-09-21-permission-rbac-module-design.md`（RBAC/权限模块设计，芙蕾雅稿）均为**设计草稿、从未提交**；洋 2026-09-24 拍板**就地入库 + 加「设计稿·未落码」页眉声明**，页眉**已由芙蕾雅直接加好**（+4 行 / +3 行），将随本批一起入库。
  ⇒ 🔴 **你不要碰这两份文件**（`git status` 里会看到它们从 `??` 变成待入库状态，那是预期，不是你造成的）。
- 本补丁的组 A 是我在验收时用**自己加的第 6 条真锁**抓出来的（spec 只要求 5 条）。教训已写进验收报告 §7：**"这条行为由另一条用例覆盖"必须实测，不能靠推理** —— 删既有测试前也要撤改一次、看它挂不挂。

---

## 6. 判据口径更正（2026-09-24 验收时确定，读本 spec 时以此为准）

本补丁**已按 spec 执行完毕并验收通过**（见 `docs/2026-09-24-命名清理批-验收报告.md` §9）。下面三条是**验收时才发现的口径问题**，**根因都在我（判据被我自己的文件/动作污染）**，交付方执行无误 —— 留在这里是为了让本 spec 单独读的时候不误导：

| # | 原判据 | 问题 | **更正后口径（实测通过）** |
|---|---|---|---|
| 1 | §1.3.2 `grep -rn '"MANUAL"' mailbots_next/tests/` ⇒ 改前 **0**、改后**恰 1** | ① 交付方 shell 会剥掉 pattern 里的双引号（退化成 `MANUAL`，多命中 docstring 行）；② **改前实际不是 0** —— 我自己跑预验证探针时留下了含 `("MANUAL","skip")` 的陈旧 `__pycache__/*.pyc` | **不依赖引号 + 跳二进制**：<br>`grep -rnI 'MANUAL' mailbots_next/tests/` ⇒ 改前 **0** / 改后**恰 2 行**（`:1058` docstring、`:1067` assert） |
| 2 | §2.3.1 `grep -rn "更名说明（2026-09-24 命名清理批）" docs/` ⇒ **恰 4 行且只在那 4 个文件** | 该串也出现在**本 spec 自身 3 处**（模板 / 示例 / 本条判据引文）与**验收报告 1 处** ⇒ 原始输出 8 行 | 豁免判据自身的文件：<br>`… docs/ --exclude='*补丁spec*' --exclude='*命名清理批-验收报告*'` ⇒ **恰 4 行（均在 :3）** |
| 3 | 头部批 §3.3.6 `grep -rnI "3\.10" . … --exclude='*命名清理spec*'` ⇒ **0** | 本补丁之后变 **1 行** = 验收报告 `:50`（表里引用了该基线命令） | 豁免扩到"所有命名清理文档"：<br>`… --exclude='*命名清理*'` ⇒ **0 行** |

🔴 **同一个教训（已升为硬规矩）**：写"期望 0 / 期望恰 N"的 grep 判据时，**必须把"将来会记录这条判据的文件"与"二进制缓存"一并排出扫描范围** —— 否则判据会在**记录它自己的那一刻**失效（恒真/恒假是同一类病）。
🔴 **附带新规矩**：跑真锁/预验证一律加 `PYTHONDONTWRITEBYTECODE=1` —— **验证动作本身会改变被验证环境**（`.pyc` 被 grep 看得见），与"mtime 不可信"同类。
